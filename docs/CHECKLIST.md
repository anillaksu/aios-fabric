# AI-OS FABRIC — CANLI İŞ LİSTESİ

**Bu dosya tek doğruluk kaynağıdır.** Her oturum burayı okur, iş bitince burayı işaretler.
Bir madde ancak **canlı kanıtı** varsa `[x]` olur — "yazdım, çalışıyordur" sayılmaz.

Son güncelleme: 2026-08-19 (Formation Memory Runtime Witness provenance kabulü) · Kanonik depo: `C:\Users\anil\Desktop\aios-fabric`

**Belge haritası:** `docs/AIOS_SESSION_START.md` = *oturuma nasıl başlanır* ·
`docs/AIOS_HANDOFF_CURRENT.md` = *özet/index, güncel durum* · bu dosya =
*ne yapacağız* · `docs/MIMARI_TEMEL.md` = *nasıl karar veriyoruz*
· `docs/STANDARTLAR.md` = *hangi standarda dayanıyoruz* · `docs/PLAN_W6_app-shell.md` = *W6 tasarımı*
· `docs/ARASTIRMA_ARSIVI.md` = *kanıtlı oluşumların yeniden kullanım indeksi*
· `docs/PRODUCT_GAP_REGISTER.md` = *açık ürün borcunun append-only kaydı*
· `docs/AIOS_DETERMINISTIC_HANDOVER.md` = *agent-bağımsız devir algoritması*.
Çakışırlarsa **bu dosya kazanır** (handoff/session-start yalnızca özet/pointer, birincil kaynak değil).

---

## 0. DEĞİŞMEZ KURALLAR (her madde bunlara uyar)

- [x] **K1 — Kanonik kaynak.** Telefondaki `~/fabric` ile depo birebir aynı olmalı; kanıt `deploy-to-phone.sh --check` → `✅` (29 dosya)
- [x] **K2 — Doğrulama kapısı.** Hiçbir iş şu üçü olmadan "bitti" değil: `BUILD_OK` + sunucu `/` **200** + davranışın **canlı tek çağrıyla** kanıtı
- [x] **K3 — AETHER protokolü.** Oturum `aether_info`/`aether_handoff` ile açılır, iş bitince `aether_append_delta` ile kapanır
- [x] **K4 — Yetki matrisi sahibindedir.** Onay katmanına ajan dokunmaz; `risk: "ask"` olan iş onaysız çalışmaz
- [ ] **K5 — Görünür doğrulama.** Test ederken cihazda **gerçekten görünür** eylemler tetiklenecek (kullanıcı ilerlemeyi görsün), ama **önceden haber verilerek**
- [ ] **K6 — Fütürizm.** Geleneksel/eskimiş çözüm reddedilir; modern, esnek, geleceğe uyumlu web platformu tercih edilir
- [ ] **K7 — Maliyet disiplini.** Gezinme/menü/pencere etkileşimi **sıfır token**; modele yalnızca gereken veri parçası gider
- [ ] **K8 — Kanıt ilkesi.** *Koddan kanıtlanmayan doğru kabul edilmez; kanıttan türetilmeyen kodlanamaz.* Dışarıdan gelen iddia (rapor, kıyaslama tablosu, tier list) önce koda sorulur
      · **Kanıt skalası dört seviyeli** (2026-08-17, `docs/MIMARI_TEMEL.md` §0.1): **FACT** (canlı çağrıyla kanıtlı) › **TEST-VERIFIED** (otomatik test var, cihazda koşmadı) › **REVIEW-VERIFIED** (yalnız kod okundu) › **TARGET** (kodda yok). Ayrım bürokrasi değil iş planı: kalan işleri farklı — TEST-VERIFIED dağıtım bekler, REVIEW-VERIFIED test bekler
- [ ] **K9 — Standart temeli.** *Standardizasyon temel alınmayan çökmeye mahkûmdur.* Kendi format icat etmek son çare; varsayılan mevcut standarda uymak — bkz. `docs/STANDARTLAR.md`
- [ ] **K10 — Öz alma.** *Yoktan var etme yoktur.* Her yapı için "bunun standart karşılığı ne?" sorusu sorulur; değer yeni format icat etmekte değil, birleşimin çalışan ilk örneği olmakta

---

## W0 — a2a.delegate kırığı ✅ TAMAMLANDI (commit `7d21a0c`)

- [x] W0.1 `dispatcher.ts`: sınıf-tabanlı dallanma → executor-varlığı tabanlı dallanma
- [x] W0.2 `a2a.delegate` self-fetch → süreç içi `A2AHub` çağrısı (`setA2AHub` enjeksiyonu)
- [x] W0.3 Timeout zinciri hizalandı (capability 25s < envelope 30s < UI 35s; `llm_bridge` 80s)
- [x] **Canlı kanıt:** PC'den gerçek `system.info` döndü, `taskId ff67e0b5` journal'da `task.completed`

## W1 — Yetki ve risk katmanı ✅ TAMAMLANDI (commit `faafee4`, `fe513a9`)

- [x] W1.1 `Capability.risk` alanı (`safe|notify|ask`, varsayılan `ask`)
- [x] W1.2 38 capability sınıflandırıldı (17 safe · 10 notify · 11 ask)
- [x] W1.3 Dispatcher'da zorunlu risk kapısı — `ask` onaysız çalışmaz, `task.failed` olarak journal'a düşer
- [x] W1.4 Otomasyon kuralı `ask` hedefleyemez (400) + zincir derinliği kesici (`MAX_CHAIN_DEPTH=3`)
- [x] W1.5 Peer başına Bearer token (fail-closed) + CORS wildcard kaldırıldı
- [x] W1.6 `pc-agent`: `shell.run` varsayılan KAPALI; `disk.free` + `proc.list` eklendi
- [x] W1.7 `link.open`/`intent.run` girdi (`q`) **ve** çıktı redaksiyonu
- [x] W1.8 `GATEWAY_KEY` → `FABRIC_GATEWAY_KEY` env
- [x] W1.9 **(sırasında bulundu)** A2A `capability:` kısayolu risk kapısını atlıyordu → kapatıldı
- [x] W1.10 **(sırasında bulundu)** `/a2a/delegate` ucu auth'suz ve risk kapısısızdı → 403
- [x] W1.11 **(sırasında bulundu)** Çalışan `pc-agent` kanonik depodan değil, eski oturum klasöründen koşuyordu → düzeltildi
- [x] **Canlı kanıt:** token'sız 401 · `ask` reddi · otomasyon 400 · `shell.run` kapalı · journal'da arama terimi yok

## W1.5-EK — Kanoniklik açığı ✅ TAMAMLANDI (commit `38ec46a`)

- [x] `aios.html`, `css/*`, `sw.js`, `manifest.json`, `vendor/`, `icons/` depoya alındı (hiçbiri izlenmiyordu)
- [x] `deploy-to-phone.sh` dağıtım + doğrulama kapsamı 17 → **29 dosya**
- [x] `.gitattributes`: ikili varlıklar `binary`, metin varlıklar LF
- [x] SSH restart askıda kalma sorunu düzeltildi (üç dağıtımda üst üste takılmıştı)

---

## W2 — A2A v1.0 protokol uyumu ✅ TAMAMLANDI (commit `3d61479`)

- [x] W2.1 `/.well-known/agent-card.json` (iki tarafta); `agent.json` alias kalır
- [x] W2.2 Agent Card tek kaynaktan üretilsin: `version` ← `package.json`, `skills` ← capability registry, **yalnızca `risk:"safe"` olanlar dışa duyurulur**
- [x] W2.3 v1 alanları: `protocolVersion`, `supportedInterfaces`, `TASK_STATE_*`, `ROLE_*`
- [x] W2.4 `GetTask` / `ListTasks` / `CancelTask` + `pc-agent`'ta JSON-RPC task kaydı (**bulundu**: hiç kaydedilmiyordu → düzeltildi)
- [x] W2.5 A2A görevlerini journal'a bağla (`a2a.task.snapshot` event'i, boot'ta replay)
- [x] W2.6 İstemci canonical yolu önce denesin (`agent-card.json` → `agent.json`)
- [x] **Canlı kanıt:** iki tarafta da `agent-card.json` 200 (`version:"0.1.0"`, B-1 kapandı) · `SendMessage`→`TASK_STATE_COMPLETED`/`ROLE_AGENT` · `GetTask`/`ListTasks` pc-agent'ta **artık buluyor** (eskiden bulamazdı) · sunucu yeniden başlatıldıktan **sonra** aynı `taskId` hâlâ sorgulanabiliyor · `CancelTask` tamamlanmış işi doğru reddediyor (`-32002`) · legacy `agent.json` alias hâlâ 200
- [x] **(sırasında bulundu)** `pollPeerTask()` ve `detectSkill()`/`execSkill()` ölü kod → silindi (B-2 kapandı)

## W3 — Asenkron teslim ✅ TAMAMLANDI (commit `c8bb47b`)

- [x] W3.1 `wait:false` yolu: iş kabul edilir, `taskId` hemen döner (**zaten vardı** — `server.ts:430`)
- [x] W3.2 Task tamamlandığında bildirim → AKTİF sekmesinde sonuç (`notifyOnComplete` + `notification.send`)
- [x] W3.3 Kalan sınırsız `fetch`'e `AbortSignal.timeout` — `a2a.ts:363` (gelen A2A'nın serbest-metin yolu, Hermes gateway'e giden çağrı, 90s)
- [x] W3.4 SSE yalnızca canlı görüntüleme; doğruluk ve nihai durum journal/task state'ten (**zaten böyleydi**, `S.tasks` `/state`'ten geliyor)
- [x] W3.5 Restart / offline / screen-locked durumda task kaybolmaz — journal replay + `markInterrupted` (**önceden vardı**); reconnect'te `/state` anında tazeleniyor (`app.js` SSE onState)
- [x] **Canlı kanıt:** `wait:false` ile gönderilen `sensor.battery.read` (`taskId 51d7e364`) anında `{ok:true,taskId}` döndü · task tamamlanınca journal'da otomatik `notification.send` task'ı görüldü (`title:"İş tamamlandı"`, `origin:system`) · `deploy --check` → 29 dosya birebir aynı
- [x] **(not)** W3.5'in "task kaybolmaz" kısmı bu oturumda YAZILMADI — `state.ts:markInterrupted` önceden mevcuttu; kod incelemesiyle doğrulandı, mid-flight crash simülasyonu (risk:ask olmayan yavaş bir capability yokluğu nedeniyle) zorlanmadı

## W4 — MCP cihaz sunucusu ✅ TAMAMLANDI (commit `9480f6f`)

- [x] W4.1 Capability registry → `tools/list`; `/mcp` Streamable HTTP (tek endpoint, POST+GET+DELETE; spec doğrulandı: modelcontextprotocol.io/specification/2025-03-26)
- [x] W4.2 Fail-closed: yalnızca `risk:"safe"` + açıkça izinli olanlar dışa açılır (`MCP_DENYLIST` genişleme noktası)
- [x] W4.3 `tools/list` ve `tools/call` **aynı** `isMcpExposed()` fonksiyonunu paylaşır
- [x] W4.4 Yetkilendirme (Bearer token + `Mcp-Session-Id`) `risk:safe` filtresinden **ayrı katman** — fail-closed, W1.5 ile aynı desen
- [x] W4.5 `tools/call` `dispatcher.dispatch()` üzerinden yürütülür — sonuç journal/task lifecycle'a bağlı
- [x] W4.6 `risk:"ask"` capability hem discovery'de yok hem doğrudan çağrıda `-32602` — **canlı doğrulandı**
- [x] W4.7 Origin doğrulaması, oturum yönetimi, GET/DELETE→405 — **canlı doğrulandı**
- [x] W4.8 Protokol hatası (`-32602`) ile tool çalışma hatası (`isError:true`) ayrımı — spec'ten doğrulanıp uygulandı
- [x] **Canlı kanıt (10 test):** auth'suz→401 · initialize→200+`Mcp-Session-Id` · oturumsuz `tools/list`→400 · `tools/list` tam 17 `safe` capability döndü (`script.run`/`whatsapp.send`/`a2a.delegate` yok) · `wifi.info` çağrısı gerçek veriyle `isError:false` · `script.run` çağrısı `-32602` (execution error DEĞİL, protokol hatası) · bilinmeyen tool `-32602` · journal'da `origin.source:"mcp"` · `GET /mcp`→405 · sahte `Origin` header→403
- [x] **(kapsam kararı, bilinçli)** Tam SSE/resumability yazılmadı — spec'in izin verdiği tek-JSON-yanıt modu seçildi; gerekçe: dışa açılan tek şey `risk:safe` (tanımı gereği hızlı/salt-okuma), SSE'nin çözdüğü "uzun süren çağrıyı güvenle tamamlama" problemi burada yok
- [x] **W4-KALICI** (commit `215e700`, `bf6b23f`) — `fabric/test/mcp.test.ts`: 4 sözleşme testi (`node:test`, dış bağımlılık yok) — tools/list↔isMcpExposed() üç yoldan tutarlılık, risk:ask ne listede ne çağrıda, protokol hatası (`-32602`) vs `isError:true` ayrımı, oturum zorunluluğu. `deploy-to-phone.sh`'e **4b) Sözleşme testleri** adımı eklendi — build'den sonra, restart'tan önce çalışır, başarısız olursa dağıtım orada durur. **Bulunan gerçek açık:** `package.json` deploy kapsamında hiç yoktu — test kapısı bunu ilk denemede yakaladı (sessizce geçmedi), kapsam düzeltildi (32 dosya)

## W5 — Deterministik action bus ✅ TAMAMLANDI (commit sıradaki)

- [x] W5.1 `llm.generate` çıktısına **sunucu tarafında** doğrulama — `fabric/src/screenspec.ts` (yeni), `renderer.js`'in aynı kuralları (bilinmeyen bileşen → elenir, izinsiz action → kaldırılır, derinlik sınırı) sunucuda tekrarlıyor; istemci kopyası **kaldırılmadı** (iki katmanlı savunma, bilinçli)
- [x] W5.2 **(bulundu ve düzeltildi)** A2A'nın `capability: X | Y` yolu hâlâ `cap.execute()`'u **doğrudan** çağırıyordu (W1.9 yalnızca aynı kontrolün bir kopyasını eklemişti, tam yolu değiştirmemişti) — artık `dispatcher.dispatch()` üzerinden yürüyor, W4'te MCP için kurulan **aynı desen**. UI/A2A/MCP/otomasyon eylemleri tek bus'tan geçer; `/read` bunun genel olmayan, yalnız `safe + readOnly` özel okuma facade'ı istisnasıdır
- [x] W5.3 Model "cihaz bilgisi" uydurması — enforcement **mimari**: dispatcher yalnızca gerçek `capability.execute()` sonucuna güvenir, model metnine asla; `prompt.ts:26` bunu talimat seviyesinde pekiştiriyor (LLM'e teknik olarak dayatılamaz, mimari sınır asıl garanti)
- [x] W5.4 Action şemasında capability/risk/hedef doğrulaması — `screenspec.ts`'teki `actionAllowed()` yapısal geçerliliği kontrol eder (capability var mı), risk kapısı **ayrı bir sınır** olarak dispatcher'da kalır (bilinçli ayrım, aşağıya bak)
- [x] W5.5 Protokol hatası vs execution hatası ayrımı W4'ten buraya da taşındı — `app.open` param eksikliği `task.failed` (execution), bilinmeyen capability/risk:ask reddi ayrı
- [x] W5.6 `validate → authorize → dispatch → journal` zinciri — UI/A2A/MCP/otomasyon eylemleri tek bus (`dispatcher.dispatch`) üzerinden bunu taşır. `/read`, yalnız açık `safe + readOnly` seti için doğrudan cihaz-okuma facade'ıdır; genel execution yolu değildir
- [x] W5.7 İdempotency — dispatcher zaten destekliyordu (`idempotencyKey`), A2A'ya bağlandı (`"a2a:"+task.id`). **Bilinen sınır (kalıcılaştırıldı, gizlenmedi):** bu yalnızca A2AHub süreci içi tekrarı engeller; çağıranın kendi `messageId`'sini taşıması gerçek uçtan-uca idempotency için gerekli, henüz plumbing edilmedi
- [x] W5.8 **Canlı+otomatik kanıt (`fabric/test/action-bus.test.ts`, 8 test):** geçerli action (kit.list tamamlandı) · eksik parametre (app.open→"pkg gerekli", yan etkisiz) · sahte cihaz bilgisi (bilinmeyen bileşen elendi, bozuk JSON bloğu silindi) · yetkisiz capability (script.run→"onay gerektirir") · doğrudan A2A action (kit.list dispatcher'dan geçti, journal'da `origin.source:"a2a"`) · A2A'dan risk:ask reddi
- [x] **W5.9 (bulundu, commit `ec331fd`)** `llm.generate` çağıranın gönderdiği `context` alanına köru köre güveniyordu — `risk:"safe"` olduğu için MCP'den dışarıdan da çağrılabiliyor (W4), yani bir MCP istemcisi sahte cihaz durumu enjekte edip modelin bunu gerçekmiş gibi kabul etmesini sağlayabilirdi. `readLiveDeviceContext()` eklendi: bağlam artık **yalnızca sunucunun kendi** `sensor.battery.read`/`wifi.info` çağrılarından geliyor, çağıranın iddiası tamamen yok sayılıyor. **Canlı kanıt:** MCP üzerinden `context:"UYDURMA-PIL-999-TAMAMEN-SAHTE"` gönderildi, model gerçek veriyle cevap verdi (`"Pil %18 ve şarj oluyor"`) — uydurma veri sisteme hiç girmedi. Regresyon testi: `fetch` yakalanıp llm_bridge'e giden gerçek istek gövdesinde sahte string'in olmadığı doğrudan kanıtlanıyor

---

## W6 — YENİ ARAYÜZ: App Shell + Pencere/Widget Sistemi ⬜ (kullanıcı vizyonu, 2026-08-17)

> Ayrıntılı tasarım: `docs/PLAN_W6_app-shell.md`
> **Zamanlama kararı: EN SONA.** W2–W5 bittikten sonra başlanır (kullanıcı: "en son bitişte").

**Kullanıcının şartları — hiçbiri atlanmayacak:**

- [ ] W6.A **Sıfır-token gezinme.** Menü açma, ızgara→tam ekran odaklanma tamamen istemci JS. Hermes'in haberi olmayacak
      *(not: sekme/ekran geçişi bugün de sıfır token — `app.js:243`; eksik olan ızgara/odaklanma katmanı. **DÜZELTME (2026-08-17):** "widget sürükleme" KARAR-1'de reddedildi — masaüstü tarzı üst üste binen pencere yok, bkz. W6.B)*
- [~] W6.B **Boş kanvas + pencere yöneticisi.** **DÜZELTME (2026-08-17, `PLAN_W6_app-shell.md §W6.2`):** madde ilk yazıldığında "çok pencereli, sürüklenebilir, z-index yönetimi" diyordu — bu, KARAR-1'in **reddettiği** masaüstü modeliydi. Düzeltilmiş kapsam: sabit **ızgara** (Android widget gibi, çakışma yok) + dokununca **odaklı tam ekrana** açılma; `WindowManager` çekirdeği (open/close/focus/persist) yüzeyden ayrık yazılır — sürükleme/boyutlandırma/z-index yığını **yok**
      · **TEST-VERIFIED** — `fabric/public/js/windowmanager.js` yazıldı: `register/focus/unfocus/remove/pin/list/onChange`, `localStorage`'a kalıcılık (enjekte edilebilir depo — Node'da bellek-içi depoyla test edildi, DOM'a **hiç dokunmadığı** kanıtlandı). 7 test: kayıt/tekil-id, odak geçişi (kayıt silinmez), kaldırma, sabitleme sıralaması, kalıcılık (yeniden yükleme), değişiklik bildirimi, **statik kanıt** — dosyada `fetch`/`XHR`/`WebSocket` çağrısı yok (K7 yapısal garanti, çalışma zamanı ölçümü değil)
      · **Güncelleme ✅ FACT (2026-08-17, aynı gün):** bir yüzeye bağlı ve **canlı kanıtlandı** — bkz. W6.C. `focus/unfocus` gerçek bir artefaktı gerçek cihazda açtı/kapattı, regresyon yok. Kalan: ızgara yerleşiminin kendisi (bugün hâlâ liste/kart görünümü, KARAR-1'in ızgarası değil) — bu W6.C'nin ikinci diliminde ele alınacak
- [x] W6.C **Boş kanvas enjeksiyonu.** ✅ **FACT.** Kabuk boş bir kutu açar, Hermes'e yalnızca "bu kutunun içi" için üretim yaptırılır — Katman A (ScreenSpec) için tamam. Katman B (serbest kod/iframe sandbox) bilinçli olarak **kapsam dışı** — W6.4/W6.E'nin işi, plan zaten öyle ayırıyor
      · **İlk dilim ✅ FACT (2026-08-17):** W6.B'nin yüzeyden bağımsız `WindowManager`'ı **gerçek yüzeye** bağlandı — `openArtifact(id)` artık `wm.register()` + `wm.focus()` çağırıyor, geri dönüş `wm.unfocus()` çağırıyor. **Yeni üretim yok** — var olan `artifactBlock()`/`render()` aynen kullanıldı. **Canlı kanıt, gerçek cihaz, gerçek artefakt ("Kablosuz Hata Ayıklama", Hermes'in bu oturumda ürettiği):** owner açtı → başlık/geri-oku/içerik doğru tam ekranda görüntülendi (ekran görüntüsü) → geri okuna dokundu → ızgaraya sorunsuz döndü ("evet oluyor"). Açılış-kapanış döngüsü ikisi de doğrulandı, regresyon yok
      · **Yan bulgu (bu doğrulama sırasında ortaya çıktı, ayrıca değerli):** `llm_bridge.py` + `watchdog.sh` Android tarafından arka planda öldürülmüştü (gateway logu: *"exited UNCLEANLY — SIGKILL/OOM/VM death"*) — telefonun daha önce 10 dk erişilemez olmasıyla aynı desen. `start_hermes_os.sh` ile yeniden başlatıldı, `llm.generate` canlı `/intent` çağrısıyla kanıtlandı (`status:"completed"`, 3.5s). **Bu W6 kapsamı dışında ama gerçek bir operasyonel risk — Android'in arka plan süreç öldürmesi tekrarlanabilir, kalıcı bir izleme/otomatik-yeniden-başlatma mekanizması yok** → yeni borç: B-9
      · **Orijinal kapsam ✅ FACT (2026-08-17, aynı gün, ikinci dilim):** "boş kutu + Hermes'in yalnızca içini üretmesi" akışı kuruldu — `createEmptyWindow()` boş bir kayıt `WindowManager`'a açar, alt composer `fillTarget` moduna geçer (placeholder: "Bu pencere için ne üretilsin?"), gönderilen istek `fillWindow()` ile **aynı pencere id'sine** yazılır (kimlik değişmez, yalnızca içi dolar). Reddedilen/başarısız üretim hiçbir yere yazılmaz (M-8 gate'i burada da geçerli)
      · **Canlı kanıt + bulunup düzeltilen gerçek regresyon:** İlk denemede var olan bir davranış (`inp.addEventListener("focus")`'taki otomatik KOMUT'a geçiş) `fillTarget` açıkken de tetikleniyordu, kullanıcının yazdığı şey yanlışlıkla normal Hermes sohbetine gidiyordu — owner canlı testte yakaladı, kök neden bulunup (`goTab("komut")` çağrısı `fillTarget`'ı temizliyordu) tek satırla düzeltildi (`!fillTarget` koruması eklendi), yeniden dağıtıldı, owner ikinci denemede doğruladı: *"artı ile açıp girince yeni artefact oluşturdum, komuta kayma yok artık düzeldi"*
      · **Yan bulgu, kök nedeni doğrulanmadı → B-10:** soğuk başlangıçta `#tabbar` görünmüyor, klavye aç/kapat ile düzeliyor. `aios.html` bu oturumda hiç değiştirilmedi — muhtemelen önceden var olan bir zamanlama sorunu, W6.I'nin sayfayı hafifletmesi (1.444→150 KB) zamanlamayı değiştirip ortaya çıkarmış olabilir ama **doğrulanmadı**, spekülasyon olarak işaretlendi
- [~] W6.D **Mikro-artefakt.** Tüm sayfa değil, yalnızca ilgili widget üretilir
      · **REVIEW-VERIFIED (2026-08-17, yeni kod yazılmadan koda sorulunca bulundu):** `extractArtifacts()` (`app.js:523`) her `\`\`\`aios` bloğunu **ayrı ayrı** `validateScreen()`'den geçirip **ayrı bir artefakt** olarak ekliyor — LLM tam sayfa değil, tek bir `{title, sections}` üretiyor; sohbet metni ayrı tutuluyor (`raw.replace(re,"").trim()`). `screenspec.ts:130` bölüm sayısını **12 ile sınırlıyor** (sert üst sınır). Bugünün canlı örneği ("Kablosuz Hata Ayıklama") bunu doğruluyor — odaklı, tek panel, tam sayfa değil
      · **Kalan gerçek boşluk:** 12 bölüm "mikro" için gevşek bir sınır — `prompt.ts`'te modele "küçük/odaklı üret" diye açık bir talimat **yok**, yalnızca kod tarafında sert bir tavan var. Bugün tipik çıktı küçük ama bu **garantili** değil, alışkanlıkla küçük. Sıkılaştırma isteniyorsa `prompt.ts`'e açık bir kısıt eklenmeli — bu FACT'e çıkmadan önce netleşmeli
- [ ] W6.E **İzolasyon.** Üretilen widget ana tasarımı/menüleri patlatamaz
      *(teknik düzeltme: Shadow DOM stil izolasyonu verir ama **güvenlik sınırı değildir** — `iframe sandbox` + `postMessage`; gerekçe plan dosyasında)*
- [~] W6.F **Kalıcı galeri + önbellek.** Üretilen widget `IndexedDB`'ye yazılır; tekrar açılışta **yapay zekaya sorulmaz**, sıfır gecikme/sıfır maliyet
      · **FACT — göç ve temel okuma/yazma (2026-08-17):** `fabric/public/js/artifact-store.js` yazıldı (`getAll/putAll/requestPersistence`, saf IndexedDB sarmalayıcı). `loadArtifacts()`/`saveArtifacts()` asenkron oldu, hedefleri `localStorage`'dan `IndexedDB`'ye taşındı — diğer **hiçbir çağrı noktası değişmedi** (`artifacts` hâlâ bellekte senkron bir dizi, yalnızca kalıcılık hedefi değişti — 4 `saveArtifacts()` çağrısı da zaten `await`'siz, bloklanmıyordu). Tek seferlik göç: IndexedDB boşsa eski `localStorage` anahtarı okunup taşınır, eski anahtar temizlenir. **Canlı kanıt:** owner sayfayı tamamen kapatıp açtı, **tüm önceki kayıtlar** (Kablosuz Hata Ayıklama, Pil Durumu, Çalışan Servisler, vb.) sıfır veri kaybıyla göründü — bu aynı zamanda `putAll()`'ın gerçek bir yazmayı (8 kayıt) başarıyla yaptığının kanıtı
      · **30 kayıt sınırı kaldırıldı** — `slice(0,30)` satırının **hem yerel repoda hem telefonun o an fiilen sunduğu `/js/app.js`'te** (canlı `curl`, `grep -c` → 0) yok olduğu doğrulandı. **Ölçekte** (30+ gerçek artefakt) hâlâ canlı test edilmedi → REVIEW-VERIFIED bu alt-iddia için
      · **`navigator.storage.persist()` çalışma zamanı sonucu (`true`/`false`) — sonuna kadar kovalandı, kesin OS sınırıyla sonuçlandı (2026-08-17):** Owner Kablosuz Hata Ayıklama'yı açtı, `adb connect` başarılı oldu. Eski başlatma betiğinin (`02-shizuku.sh`) kullandığı sınıf (`ServerStarter`) bu Shizuku sürümünde **artık yok** (`ClassNotFoundException` — APK indirilip `strings classes.dex` ile doğru sınıf, `ServiceStarter`, bulundu). O da tek başına `-D` sistem özellikleri olmadan "starting service **null/null**" ile başarısız oldu. Owner Shizuku uygulamasını kendi arayüzünden başlattı — `rish -c id` başarıyla **`uid=2000(shell)`** döndürdü, Shizuku gerçekten çalışıyor. Ama Chrome'un veri dizinini okumaya çalışınca (`ls /data/data/com.android.chrome/`) **`Permission denied`** — `getenforce` → `Enforcing`, `getprop ro.build.type` → `user` (üretim derlemesi). **Kesin sonuç:** Android'in SELinux politikası üretim derlemesinde `shell` UID'ini bile başka bir uygulamanın özel verisine erişimden engelliyor; Shizuku root vermiyor, yalnızca shell yetkisi veriyor, o da bu sınır için yetersiz. Root olmadan aşılamaz. **TARGET kalıyor ama artık üç ayrı katmanda (Shizuku başlatma zinciri: port→sınıf adı→sistem özellikleri, sonra SELinux) somut kanıtla tükendi** — kalan tek yol cihazı root'lamak, ki bu ayrı ve çok daha büyük bir karar
      · Sunucu senkronu (`/artifacts` POST) **korundu**, dokunulmadı
- [ ] W6.G **Uygulamaya dönüştürme.** Galeriden ana ekrana sürükle → kalıcı "uygulama"; yayınlama yolu
- [~] W6.H **Dar context.** Widget içi işlemde modele yalnızca o widget'ın verisi gider, tüm uygulama durumu değil
      · **Bulgu (2026-08-18, yeni kod yazmadan koda sorulunca):** `fillWindow()` (W6.C) zaten `history` göndermiyordu — sunucu tarafında (`capabilities.ts:1033-1036`) boş `history` hiçbir önceki sohbet turunu modele eklemiyor. Ama **"YENİLE"** (bir artefaktı yeniden üretme) hâlâ tam `ask()` yolundan geçiyordu — son 7 sohbet mesajını taşıyordu, gereksiz
      · **TEST-VERIFIED — düzeltildi:** `ask()`'a `opts.narrow` eklendi (`history` boş kalır, `goTab("hermes")`'e zıplama yok); YENİLE artık `ask(a.prompt, {silent:true, narrow:true})` çağırıyor. Build+test geçti (26/26), telefona dağıtıldı, md5 birebir
      · **Kalan iş — FACT'e çıkması için:** telefonda bir artefaktı YENİLE'ye basıp Hermes sekmesine sıçramadığının canlı görülmesi (K5)
      · **Kapsam dışı bırakılan, ayrı bir konu:** YENİLE'nin aynı artefaktı güncellemek yerine **yeni bir id ile yeni kayıt** oluşturması (mevcut, bu oturumdan önceki davranış) — bu bir "dar context" sorunu değil, ayrı bir tasarım sorusu, dokunulmadı
- [x] W6.I **Framework7'yi at.** ✅ **FACT** — tam kanıtlandı (2026-08-17)
      · Canlı ölçüm (telefon `:9300`, HTTP 200): sayfada `framework7-bundle` referansı **0**; kabuk yükü **1.444 KB → 150 KB (%90 düşüş)**. `app.toast`/`app.sheet` çağrısı kodda **0**, `Framework7` kelimesi **0**
      · `BUILD_OK` + 12/12 sözleşme testi (PC'de **ve** telefonda), md5 birebir (34 dosya)
      · **Görsel kanıt (K5):** owner telefonda Control Center'a dokundu, panel açıldı → native `<dialog>.showModal()` Android 15 WebView'de çalışıyor. Eski Framework7 `sheet`'i ~30 satır native kodla birebir değiştirildi
      · Not: bundle dosyaları telefonun diskinde **duruyor** (`vendor/` bilinçli olarak md5 kapsamı dışı — B-4); sayfa yüklemiyor ama sunulabiliyorlar → B-8
- [ ] W6.J **Fütürist temel.** Web Components + CSS Container Queries + View Transitions API + `dvh`/`env()` (zaten kısmen var)

**Tier list denetiminden gelen ekler (2026-08-17):**

- [x] W6.K **Web Worker çekirdeği.** ✅ **FACT (2026-08-18) — canlı kanıtlandı.** Üretilen kod Worker'da koşar (izole + `terminate()` edilebilir → kaçak widget telefonu kilitlemez); Worker `ScreenSpec` üretir, çizimi ana thread yapar
      · **Sınır (zaten karar, `PLAN_W6_app-shell.md §1b` adım 4'te yazılı — burada çapraz referans için tekrarlanıyor):** Worker yalnızca compute/transform/parse yapar, **privileged capability çağıramaz**. Üç yol ayrı kalır: `UI → sandbox/native çizim` · `compute → Worker` · `privileged capability → dispatcher/policy`. Worker capability *ister*, W1 risk kapısından geçirilmeden asla çalışmaz — W6.4'ün postMessage köprüsüyle aynı kural
      · **TEST-VERIFIED (2026-08-18):** `extractArtifacts()`/`actionableCount()` (LLM ham metninden `\`\`\`aios` bloğu ayıklama + `validateScreen` + M-7 sözleşme kapısı `admitArtifact`) `public/js/artifact-parse.js`'e (saf compute, DOM yok) taşındı; `public/js/parse-worker.js` bunu izole bir Worker içinde çalıştırır (`self.onmessage`/`postMessage`, hiçbir `capability.execute()`/dispatcher erişimi yok — statik olarak doğrulanabilir, dosyada öyle bir çağrı yok). `public/js/parse-client.js` ana thread tarafında Worker'ı yönetir: `WindowManager`'ın depo enjeksiyonuyla aynı desenle (`createWorker` enjekte edilir) Node'da **gerçek Worker olmadan** test edilebiliyor. `app.js`'teki `ask()`/`fillWindow()` artık senkron yerel `extractArtifacts`/`admitArtifact` yerine `await parseClient.parse(...)` çağırıyor
      · **10 yeni test** (`parse-worker.test.ts`, 46/46 toplam geçti): geçerli/reddedilen artefakt ayıklama, ACTIONABLE boşken fail-open, bozuk JSON, derinlik sınırı, başarılı round-trip, **kaçak/asılı worker timeout içinde `terminate()` edilir** (K.7'nin iddiasının doğrudan kanıtı), worker kendi çökünce (`onerror`) bekleyenler reddedilir, terminate sonrası worker yeniden kurulur
      · **Canlı kanıt (K5, owner, 2026-08-18):** iki ayrı yol da denendi — HERMES sekmesinden yazınca `ask()` yolu (Worker'da parse+admit) sıfırdan artefakt üretti; ARTEFAKT'ta yeni boş pencere açıp yazınca `fillWindow()` yolu (aynı Worker) anında doldurdu. İkisi de eskisiyle aynı davranışı gösterdi, hata/donma yok — refactor davranışı bozmadı
      · **Kapsam dışı bırakılan, ayrı doğrulama:** DevTools Performance/Threads panelinde ayrı Worker thread'inin görsel teyidi ve kasıtlı bozuk/sonsuz döngülü bir `\`\`\`aios` bloğuyla ana thread'in kilitlenmediğinin sahnelenmesi — fonksiyonel FACT için gerekli değildi (ParseClient'ın timeout+terminate() testi zaten bunu otomatik kanıtlıyor), istenirse ayrıca yapılabilir
- [x] W6.L ✅ **FACT (2026-08-18) — canlı kanıtlandı.** Prompt→kod önbelleği. SHA-256 (`crypto.subtle`) ile eşleme; normalizasyon zorunlu, hash'e capability sürümü katılır.
      · **Kanıt yolculuğu (dürüstçe kaydedildi):** ilk iki canlı denemede "ikinci sefer de bekledi" raporlandı. Kök neden **önbellek değildi** — sessizce yutulan hatalar teşhisi engelliyordu (bkz. sessiz-catch denetimi commit'i) VE ayrıca **B-9 üçüncü kez tekrarladı** (`llm_bridge` çökmüştü, journal'da `task.failed: "fetch failed"` görüldü, port 9201 kapalıydı). Servisler yeniden başlatılıp backend canlı `curl` ile doğrulandıktan **sonra** üçüncü denemede: *"ikinci sefer anında geldi"*
      · **Kapsam kararı (owner, 2026-08-18):** L1 — yalnızca **güvenli** canonicalization (küçük harf + Unicode NFKC + boşluk sadeleştirme). Dolgu kelimesi silme (**"bana"/"lütfen"/"yapar mısın"**) **bilinçli olarak yok** — yanlış pozitif riski (farklı niyetli iki isteğin aynı hash'e düşüp **yanlış** artefaktı doğrudan sunması) kabul edilemez. Gerçek kullanım verisi tekrar eden örüntüleri kanıtlarsa L2'de kontrollü genişletilir
      · **Anahtar dört bileşenli (owner'ın uyarısı üzerine genişletildi):** `normalizedPrompt · capabilitySetVersion(ad+RİSK) · registryVersion(bileşen kataloğu) · modelProfile`. Herhangi biri değişirse eski (olası geçersiz) üretim **asla** yanlışlıkla geri dönmez — örn. bir capability'nin risk'i `safe→ask` olursa isim kümesi aynı kalsa bile hash değişir
      · **`{structureHash, structure, parameters}` ayrımı bilinçli olarak L1'e dahil edilmedi** — bugünkü ölçekte (birkaç düzine artefakt) gerçek fayda kanıtlanmadı, W6.5d'nin erken mühendislik reddiyle aynı gerekçe
      · **FACT — sunucu tarafı:** `/capabilities` artık `risk` alanını dönüyor (canlı doğrulandı); `/prompt-cache-hit` gerçek bir olayı journal'a yazdı (`type:"prompt.cache.hit"`, canlı `curl` ile kanıtlandı)
      · **10 test** (`prompt-cache.test.ts`, 2026-08-18 revizeyle 7→10): normalizasyon kelime silmiyor (bilinçli), Unicode NFKC eşdeğerliği, anahtar deterministik, risk değişince anahtar değişiyor, capability seti değişince anahtar değişiyor, sıra anahtarı etkilemiyor, **`writeEligible()` güvenlik sınırı** (aşağıya bkz.), çapraz-yüzey aynı anahtar senaryosu
      · **Canlı kanıt (K5, üçüncü denemede):** owner ARTEFAKT'ta aynı isteği iki kez yazdı, ikincisi **anında** geldi — `getCached`/`putCached` (IndexedDB) gerçekten çalışıyor
      · **REVİZE (2026-08-18, owner kararı) — `canWrite` artık `history.length` değil `writeOrigin`'e bağlı.** Canlı kullanımda bulundu: HERMES sekmesinde 2. mesajdan sonra `history` bir daha hiç boşalmıyor, bu yüzden eski kural (`cacheable = history.length===0`) cache'i sohbet boyunca kalıcı olarak kapatıyordu — bağımsız/birebir tekrarlanan bir istek bile bir daha asla önbellekten dönmüyordu. **Bu bir DAG/Artifact Compiler eksikliği DEĞİL** (doğrulandı: `cacheKey()` zaten hiçbir ScreenSpec ağacı/yapısal dedup kullanmıyor, düz metin hash'i) — saf bir cache-eligibility/request-identity sorunuydu. **Yeni model:** OKUMA (`getCached`) her zaman kayıtsız şartsız denenir (`cacheKey` zaten `history` taşımıyor — yalnızca `normalizedPrompt·capSig·regVer·model`); YAZMA (`putCached`) yalnızca `opts.trustedWrite===true` işaretli **bağımsız/standalone üretim kaynaklarından** yapılır (`public/js/prompt-cache.js:writeEligible()`) — ARTEFAKT boş pencere (`fillWindow`, hep kayıtsız şartsız yazar) ve `YENİLE` (narrow, kendi kayıtlı orijinal prompt'unu tekrar çalıştırır → `trustedWrite:true`). Sohbet-içi genel `ask()` çağrıları (normal HERMES mesajı, `ui.ask` retry, `ui.miniapp`, share-intent) **varsayılan olarak yazamaz** — `"evet"`/`"onu değiştir"` gibi bağlama bağlı kısa mesajların cache'e hiç girmemesini garanti eden asıl sınır bu (byte-exact eşleşme + güvenilmeyen yazma kaynağı, çift katmanlı). **3 yeni test:** `writeEligible(undefined/false)===false`, `writeEligible(true)===true`, ARTEFAKT'ta yazılan istekle HERMES'te aynı normalize istek aynı `cacheKey`'e düşüyor (çapraz-yüzey reuse'un temeli — IndexedDB round-trip'in kendisi Node'da test edilemiyor, canlı cihazda kanıtlanır). **Canlı kanıt (K5, owner, 2026-08-18, revize sonrası):** **(A) çapraz-yüzey HIT** — ARTEFAKT'ta boş pencerede "basit hesap makinesi paneli" üretildi (standalone, `trustedWrite`), sonra HERMES sekmesinde AYNI metin yazıldı → anında geldi (0 token, LLM'e gitmeden) — `READ`'in kaynaktan bağımsız çalıştığının kanıtı. **(B) güvenli WRITE sınırı** — HERMES'te bağlamsız "evet" iki ayrı seferde de normal şekilde LLM'e gitti, cache'den yanlışlıkla dönmedi — sohbet-içi genel `ask()`'ın `canWrite`'ının gerçekten kapalı kaldığının kanıtı
- [x] **W6 UI Expressiveness — Layer A ilk referans dilimi.** ✅ **FACT (2026-08-18).**
      · ScreenSpec contract'a yalnız `stack`, `scroll-region`, native `range` eklendi; sunucu ve istemci validator'ları aynı alan/binding sınırlarını uygular, registry drift testi katalog eşitliğini korur. `range` için `input` yalnız yerel değeri günceller; `change` bırakışında action payload'a `valueKey` ekleyip tek dispatcher eylemi üretir.
      · `meetsUiRequirements(spec, requirements)` saf/deterministiktir; doğal dili yorumlamaz. Referans `reference-sound-panel-v1`, `scroll-region → stack → range` ve `capability:volume.set` gereksinimlerini bu kapıdan geçerek kalıcılaştı.
      · **Test/deploy:** kod zinciri `5873af2 → 82a5f6b → 5905039`; `npm test` 75/75, BUILD_OK, telefon dağıtımı ve depo-telefon eşitliği geçti.
      · **Canlı K5:** owner telefonda iç-scroll, parmakla range sürükleme, anlık yerel değer ve yeniden açılışı gördü. Bırakış `music=10` için tek task zinciri üretti: `intent.received → intent.dispatched → task.created → task.completed` (`taskId 624f3358-5582-49cc-b2e3-1df2a874ebb5`). Dispatcher üzerinden ayrı `volume.read` gerçek cihazda `music=10` döndürdü. `volume.read` `readOnly` değildir; bu yüzden `/read` yerine dispatcher kullanıldı.
      · **Kapsam sınırı:** Bu yalnız Layer A deklaratif UI dilimidir; Layer B/sandbox, yeni wire protocol, DAG/compiler, pub/sub ve shared state açılmadı. W6.G'nin ayrı lifecycle statüsü bu maddeyle FACT'e yükselmez.
      · **Medya-volume state dilimi (FACT, 2026-08-18; kod `82d694b`):** Referans panel açılışında yalnız mevcut `volume.read` dispatcher zarfıyla okunur; `termux-volume` sonucundaki doğrulanmış `music.volume` ve `music.max_volume` saf mapping ile mevcut `range` label/value/max alanlarına uygulanır. Geçersiz/missing `music` cevabı sahte sayı/slider üretmez, empty state kalır. Mapping testi eklendi; toplam **77/77**. Telefonda kullanıcı slider'ı 49→91→150→68 ve sonra 150 değerlerine bıraktı; her bırakış kendi tek `volume.set` task'ı olarak tamamlandı. Yeniden açılıştaki `volume.read` sırasıyla 68/150 döndürdü ve slider gerçek cihaz değeriyle kuruldu (`taskId 0d7c05ed-98f1-484b-833f-223db8046610`, sonra `d0755254-9b75-46d1-b15c-d3eb241eed53`). `media.control.pause` aynı canlı oturumda Shizuku kapalı olduğu için fail-closed kaldı; oynatılıyor/parça/app metadata'sı için FACT iddiası yoktur.
      · **Medya kontrol hizalaması (FACT, 2026-08-18; kod `eaf2a64`):** Referansın eski `DURAKLAT → pause` bağı, mevcut capability'nin gerçek play/pause semantiği olan `OYNAT / DURAKLAT → media.control({action:"toggle"})` ile değiştirildi. Deterministik admission `capability:media.control` da ister. Shizuku tekrar etkinleştirildikten sonra owner üç düğmeyi telefonda gerçek medya üzerinde doğruladı; journal'da `toggle` (`375353dc-6dcd-47ad-a962-d317464f54c4`), `next` (`9819d06d-239e-44f6-8ccf-726163b2c413`) ve `prev` (`2a85add1-b9b1-4882-835c-d49f5df67424`) ayrı `task.completed` olarak kaydedildi. Bu, oynatma metadata/state'i okunduğu anlamına gelmez.
- [x] **W6 Phone Workspace — HOME + KEŞFET + Cihaz Durum Merkezi.** ✅ **FACT (2026-08-18; kod `6b97613`, `/read` eklemesi `d5644f6`).**
      · HOME'daki ÇALIŞMA ALANI ve KEŞFET aynı istemci-yerel, deterministik katalogdan Cihaz/Medya/Ağ/Uygulamalar/Sistem/AIOS/Araçlar yüzeylerini üretir. Türkçe kısa arama `cih`, `pil`, `ağ`, `ses` gibi başlık/tag eşleşmelerinde çalışır; LLM çağrısı ve yeni wire protocol yoktur. Artifact, ApplicationEntry, sistem ekranı ve Android uygulaması ayrı rolünü korur.
      · `Cihaz Durum Merkezi` kalıcı `reference-device-status-v1` referans artifact'ıdır. Açılışta mevcut UI → envelope → `dispatcher.dispatch()` zincirinden `sensor.battery.read`, `wifi.info`, `app.list` okur; yalnız gelen gerçek alanları ekrana map eder. Canlı journal: battery `8e091ec8-61f8-4967-adb1-ae4811a9c82c`, Wi-Fi `0c298b19-bd00-4b7d-a828-8587eab6b5ea`, uygulama listesi `cfc95b7-f23ab-4845-bcf4-d0727af3734c` — sonuncu 63 uygulama/52 ad döndürdü. Hermes/Gateway için canlı health kaynağı yoktur; sahte sağlıklı servis bilgisi gösterilmez.
      · **Test + canlı kabul:** `workspace-catalog.test.ts` ile birlikte `npm test` 81/81 ve syntax/diff kontrolleri geçti. Owner telefonda HOME → kategori → KEŞFET, kısa arama, Cihaz Durum Merkezi'nin gerçek verisi ve referansın tekrar açılışını doğruladı. Bu kanıt W6.G'nin ayrı application-lifecycle durumunu değiştirmez.
- [x] **W6 Phone Workspace navigation — ortak back/history.** ✅ **FACT (2026-08-18; kod `c66e5da`).**
      · Yeni router yok: `navigation-state.js` yalnız aynı-belge browser state'ini normalize eder. Tab seçimi history adımı üretmez; kategori/secondary ekran ve odaklı artifact geri dönülebilir adımdır. Artifact odaktayken secondary filtre taşınmaz; browser/Android geri hareketi eski WindowManager focus'unu kapatır. Native sheet/dialog kendi cancel/close semantiğinde kalır, navigation ile karışmaz.
      · Tüm secondary ScreenSpec yüzeyleri ortak geri başlığı alır; touch target 42px'tir. Uygulama/artefakt geri akışı geldiği yüzeye döner, sabit ARTEFAKT sekmesine zorlanmaz.
      · **Test + canlı kabul:** saf state contract testleri ile `npm test` 83/83; telefon BUILD_OK, 83/83 ve 65 dosya md5 eşitliği geçti. Owner telefonda kategori → Cihaz Durum Merkezi/artefact → geri, ikinci geri ile KEŞFET, tab değişimi ve Android/browser geri jestini doğruladı.
- [x] **W6 Phone Workspace motion — native View Transition + reduced motion.** ✅ **FACT (2026-08-18; kod `71688e1`).**
      · Yeni animasyon kütüphanesi yok: mevcut `--t-fast`/`--t-base` tokenlarıyla aynı-belge View Transition API kullanılır. Navigation push/pop/tab türleri kısa, yönlü geçişe map edilir; destek yoksa doğrudan render edilir. Geçiş kesilirse görünür tanı kaydı bırakılır; sessiz catch yoktur.
      · `prefers-reduced-motion: reduce` hem adapter'da API geçişini atlar hem CSS hareket/sürelerini anlıklaştırır. Bu preference kullanıcı seçimi olarak korunur.
      · **Test + canlı kabul:** `view-transitions.test.ts` ile `npm test` 85/85; telefon BUILD_OK, 85/85 ve 67 dosya md5 eşitliği geçti. Owner telefonda tab, kategori, artifact giriş/geri geçişlerini ve reduced-motion davranışını doğruladı.
- [x] **W6 Phone Workspace visual tokens + focus.** ✅ **FACT (2026-08-18; kod `06f3128`).**
      · Mevcut PHS/AMB/ICE/SYN/PPR temaları korunarak component niyet tokenları (`surface`, `text`, `border`, `success`, `focus`) merkezi hale getirildi. Yeni ekranlar ham renk/ayrı gray üretmez. `--touch-target`, `--control-h`, `--page-pad`, `--section-gap` ortak ölçülerdir.
      · Klavye erişimi için görünür, tema-duyarlı `:focus-visible` halkası; composer eylemlerinde 44px dokunma alanı eklendi. Yeni component paketi veya tema motoru yoktur.
      · **Test + canlı kabul:** `design-tokens.test.ts` ile `npm test` 86/86; telefon BUILD_OK, 86/86 ve 68 dosya md5 eşitliği geçti. Owner telefon yüzeylerinde tema tutarlılığı, dokunma alanı, focus halkası, okunurluk ve küçük ekran taşmasını doğruladı.
- [x] **W6 Phone Workspace HOME recent ApplicationEntry.** ✅ **FACT (2026-08-18; kod `9c337a1`).**
      · ApplicationEntry açılışında yalnız entry üzerinde `lastOpenedAt` tutulur; artifact spec'i, artifact metadata'sı, capability veya execution state'i değişmez. HOME, ÇALIŞMA ALANI'nın altında en fazla dört `SON KULLANILAN UYGULAMALAR` girişini son açılış zamanına göre gösterir.
      · **Test + canlı kabul:** ApplicationEntry model ve HOME ScreenSpec contract testleriyle `npm test` 88/88; telefon BUILD_OK, 88/88 ve 68 dosya md5 eşitliği geçti. Owner iki ApplicationEntry açıp HOME'a döndü; doğru artifact'in açıldığı ve en son açılanın üstte göründüğü doğrulandı.
- [ ] W6.M **Deterministik prompt şablonları.** Serbest istek değil, sınırları çizilmiş görev; sabit iskelet + değişken yuvalar
- [ ] W6.N **Pub/sub yetki modeli.** Widget A, widget B'nin olaylarını dinleyebilir mi? Kanal başına izin — "secure" kelimesinin karşılığı
- [ ] W6.O **Widget kalıcı verisi.** Widget başına alan mı, paylaşılan depo mu; paylaşımlıysa yetkiyi kim verir
- [ ] W6.P **Yaşam döngüsü ve bellek.** Pencere kapanınca timer/listener/Worker temizliği + bellek eşiği
- [ ] W6.R **Bozuk sürümden dönüş.** v2 bozuksa v1'e geri alma; yayınlanan widget'ı geri çekme
- [ ] W6.S **Çevrimdışı davranış.** Kod yerelden gelir ama capability çağrısı başarısız olur — widget bunu nasıl gösterir
- [x] W6.T **`sw.js` güncellemesi.** ✅ **FACT** (2026-08-17) — canlı `GET /sw.js` telefondan: `SHELL = "aios-shell-v6"` (v5'ten yükseltildi), `SHELL_FILES`'tan iki F7 bundle'ı çıkarıldı. Sürüm yükseltmesi şart: `activate` handler'ı `SHELL`'den farklı tüm cache'leri siliyor, yani eski v5 önbelleğindeki 1.27 MB bundle bir sonraki açılışta temizlenir — karma sürüm riski kapandı
- [ ] W6.U **Erişilebilirlik.** Pencere klavyeyle taşınabilmeli; ekran okuyucu yığını anlatabilmeli
- [ ] W6.V **Performans bütçesi.** Telefonda aynı anda kaç pencere — sayı konmadan "iframe ağır" tartışmasının hakemi yok
- [ ] W6.Y **Üretilen kodun denetimi.** Kayda geçmeden statik kontrol (W5 şeması ScreenSpec'i kapsıyor, serbest kodu kapsamıyor)
- [ ] W6.Z **AETHER'a kayıt.** Widget üretimi yönetişim hattında görünsün
- [x] W6.W ✅ **FACT (2026-08-18) — M-7 ile kapandı, farklı konumda.** Capability minimal closure — üretilen widget yalnızca gerçekten kullandığı capability'leri bildirir. `artifact-contract.js:usedActionTypes()` ScreenSpec ağacını tarayıp gerçekten kullanılan action tiplerini çıkarır, `admitArtifact()` yalnızca bunları `capabilities` alanına yazar (bildirilen değil, kullanılan). **Konum farkı dürüstçe not:** madde `screenspec.ts`'e (sunucu) taşınmasını öngörüyordu, uygulama istemci tarafında (`artifact-contract.js`) oldu — M-9 sonrası sunucu birincil kaynak olsa da, kabul kararı (admit/reddet) hâlâ istemcide veriliyor. İşlevsel sonuç aynı, canlı kanıtlı (M-7'nin testleri + telefon doğrulaması)

- [x] **Formation Memory — Runtime Ledger provenance köprüsü** ✅ **FACT (2026-08-19).** `6e2e314` Runtime Ledger'ın `GENESIS → exact previous-event-hash → event-hash` zinciri geçerliyse, yalnız `task.completed` sonucu için redakte edilmiş sonuçtan `resultDigest` üreten canonical `RuntimeWitness` ve immutable provenance edge yazar. Parent yalnız artifact deposundaki exact, doğrulanmış formation kimliğiyle eşleşir; başlık/prompt/similarity kabul edilmez. `dispatcher` tamamlanma kancası production çağrı noktasıdır; heartbeat/watchdog turu formation/edge üretemez. Root formation mutasyona uğramaz: aynı formation'ın yeni bir execution bağlamında kullanımı **reuse**'dur, derived child formation değildir.
      · **JOIN / conflict sözleşmesi:** edge kümesi idempotent, commutative ve associative set-union'dır; duplicate/replay tek edge'e iner, sıra canonical projeksiyonu değiştirmez. Aynı edge ID için farklı canonical içerik fail-closed reddedilir; çözüm politikası eklenmedi.
      · **Test kanıtı:** `3bbca13` reuse≠derived kabulünü ekledi. PC `npm test` **144/144 PASS**, `BUILD_OK`; telefonda aynı revision **144/144 PASS**, repo=telefon **96 dosya md5 eşit**, odaklı runtime-provenance paketi **13/13 PASS**. Testler chain-break, exact parent, duplicate/replay, X→Y/Y→X + duplicate replica convergence, semantic equality ile canonical serialization hash equality ve conflict rejection'ı ayrı doğrular.
      · **Canlı kanıt (telefon):** kalıcı `reference-sound-panel-v1` exact formation'ı `formation:53542f…030c655`, ikinci ayrı `volume.set({stream:"music",value:10})` task'ında (`9a030eee-fa1d-4a3b-ae91-0d5f34ed7804`) yeniden kullanıldı. `task.completed` sonrası witness `runtime-witness:23952…6bcaa` ve yeni immutable edge `provenance-edge:025faa…ec3ad` yazıldı; önceki canlı edge ile aynı parent, farklı task/witness/edge kaldı. Son `volume.read` cihazda `music=10/150`; ledger `LEDGER_OK events=42`. Telefon export paketi PC'de import edildi: **1 formation + 2 edge**, semantic projection ve canonical serialization hash ikisinde de `sha256:93e50a…07ee8`. Bu, iki fiziksel bağlam arasında portability kanıtıdır; iki telefonun bağımsız execution ürettiği iddia edilmez.
      · **Sınır:** canlı çağrı Fabric `/envelope` üzerinden gerçek dispatcher yürütmesidir; bu maddede fiziksel PWA range'e parmak dokunuşu ayrıca iddia edilmez. B-9 Android/MIUI survivability riski bu FACT'i genişletmez ve açık kalır.

**Artifact Compiler/Optimizer — kapsam kararı (2026-08-17):** Kullanıcı tam bir derleyici hattı önerdi (canonical IR, DAG, terfi yaşam döngüsü, çok aşamalı GC, maliyet-tabanlı reuse). Ölçüldü: telefonda **8 artefakt, 5.9 KB**. Karar: **ERTELENDİ, silinmedi** — ayrıntı ve yeniden gözden geçirme tetikleyicisi `docs/PLAN_W6_app-shell.md` §W6.5d'de. Ucuz/gerçek değerli üç parça şimdi alındı: exact dedup (zaten W6.L'de vardı), structure+parameters ayrımı (W6.L'ye eklendi), capability minimal closure (yukarıda W6.W).

**Kod yazımından önce cevaplanan kararlar — ÜÇÜ DE ÇÖZÜLDÜ (2026-08-17):**

- [x] **KARAR-1 — ÇÖZÜLDÜ: Hibrit.** Ana ekran sabit izgara (Android widget'ı gibi, çakışma yok); bir widget'a dokununca **odaklı tam ekrana** açılır (masaüstü tarzı üst üste binen pencere yok — telefon ekranına uygun). `WindowManager` mantığı yüzeyden ayrık yazılır (surface-agnostic) — gelecekte başka bir yüzey eklenirse bu karar tek başına değişebilsin diye, ekstra iş değil sadece doğru modül sınırı
- [x] **KARAR-2 — ÇÖZÜLDÜ: tek-seferlik onay + kapsam-değişince-yeniden-sor.** Owner'ın ikili "ask/notify" çerçevemi düzelttiği model: `NEW ARTIFACT → validate → risk=ask → USER APPROVAL → persist artifact+approval-scope → future reuse → NO NEW ASK`. Aynı doğrulanmış artefakt tekrar açılırsa **0 token, 0 tekrar onay**; widget YENİ bir capability istemeye başlarsa (kapsam genişlerse) **onay tekrar sorulur**. Mobil uygulama izin modeliyle aynı mantık; W6'nın sıfır-token hedefiyle uyumlu — onay maliyeti yalnızca GERÇEK yeni riskte ödenir
- [x] **KARAR-3 — ÇÖZÜLDÜ (2026-08-17).** ~~"Yerel llm_bridge zaten var, harici API veriyi dışarı taşır"~~ **YANLIŞ İDDİAYDI, düzeltildi:** `docs/RESUME.md:744-761`e göre `llm_bridge.py` zaten `--provider openai-codex -m gpt-5.6-luna` ile Codex OAuth üzerinden kullanıcının ChatGPT/OpenAI hesabına gidiyor — **hiç yerel değil**, zaten harici. Owner: bu hesabı hem mobilde hem PC'de zaten kullanıyor; ileride aynı Tailscale ağında barındırılan **OmniRoute** (`aether://project/omniroute`, "Free-First AI Gateway Router") üzerinden model servis edecek. **Karar:** Katman B için şimdi yeni bir model entegrasyonu YAPILMAZ — mevcut Hermes gateway (zaten çalışan hesap) kullanılır; OmniRoute hazır olduğunda yönlendirme oraya taşınır (ayrı proje, ayrı zaman çizelgesi). Tier list'in rastgele üçüncü-taraf DeepSeek/GPT-4o önerisi hâlâ reddedilir — ama gerekçe "yerel/ücretsiz" değil, "kullanıcının kendi kontrolündeki router projesi zaten yolda"

---

## S — Standardizasyon maddeleri ⬜ (`docs/STANDARTLAR.md`)

- [ ] **S-1** CloudEvents **adapter** — journal iç formatını korur, dışa açılırken map edilir ("yeni event standardı icat ettik" denmez)
- [ ] **S-2** W3C Trace Context — `traceparent`/`tracestate` zarfa **eklenir**; `correlationId`/`causationId` yerine geçmez (farklı kavramlar)
- [ ] **S-3** ScreenSpec ↔ **A2UI** hizalaması — A2UI'nin "güvenilir bileşen kataloğu" deseni bizde zaten var (`renderer.js:22`); adapter yazılırsa FACT, yazılmazsa TARGET
- [ ] **S-4** Widget köprüsü: JSON-RPC 2.0 **mesaj biçimi** + postMessage **binding** + AIOS widget **semantiği** (yeni wire protokol yok)
- [ ] **S-5** ScreenSpec'ler MCP **resource / App** olarak yayınlansın (W4 ile)
- [x] **S-6** ✅ **FACT (2026-08-18, doğrulanarak kapandı)** — `STANDARTLAR.md §2`'deki "Durum tablosu" zaten tam bu formatta (`Özellik | Kod durumu | Standart / hedef`) ve kural orada açıkça yazılı (`"Bundan sonra her mimari tablo bu üç sütunu taşır"`). Yeni bir şey yazılmadı, var olan kontrol edildi
- [ ] **S-7** Capability eşlemesi **katmanlı** olsun: MCP Tool / MCP Resource / A2A Skill / Device Capability / Worker Capability — birebir `capability = MCP tool` değil
- [ ] **S-8** Sandbox için **ayrı origin** ayrılsın — MCP Apps şartı (`allow-scripts allow-same-origin` ancak farklı origin'de güvenli); W6.3 gereksinimi

## M — Mimari temelden doğan maddeler ⬜ (`docs/MIMARI_TEMEL.md`, 2026-08-17)

> Owner'ın mimari değerlendirme raporu koda soruldu (K8) ve sabitlendi. Aşağıdakiler
> o denetimden **yeni** çıkan işler; raporun geri kalanı mevcut K6-K10 ve S-1..S-8'i
> teyit etti (yeni iş doğurmadı). **M-7/M-8:** owner'ın 2026-08-17 ikinci turdaki
> denetimi (Artifact Contract'ın kapı olması, ephemeral/persistent ayrımının
> koda yansıması) — M-5'i tamamlıyor, bkz. aşağı.

- [ ] **M-1** Mevcut maddeler dört seviyeli kanıt skalasına göre yeniden etiketlensin (§0.1) — başlangıç: W6.I `TEST-VERIFIED`, W3.5 `REVIEW-VERIFIED`
- [x] **M-2** **B-6 kapandı** — bkz. B-6 detayı. W6 kod yazımı artık drift korumasıyla başlıyor
- [ ] **M-3** Maliyet ölçümü **beş kalemli** olsun: token · gecikme · ağ · doğrulama işi · cihaz enerjisi. W6.7 bugün yalnızca token sayıyor; K7'nin gerçek konusu beşi birden
- [ ] **M-4** Korpus artefaktları `provenance: "corpus"` ile işaretlensin ve W6.5d'nin **200 artefakt tetikleyicisinden hariç tutulsun** — aksi hâlde sentetik korpus kendi tetikleyicisini ateşler ve ertelenen derleyiciyi erken açtırır (§11)
- [~] **M-5** Artefakt sözleşme alanları W6.F şemasına girsin: `approvalScope` (KARAR-2) + `capabilities` (W6.W) + `version` + `provenance` (§4).
      · **FACT (2026-08-17) — `capabilities`/`version`/`provenance` kısmı:** `fabric/public/js/artifact-contract.js` yazıldı, `app.js`'e bağlandı (`boot()`'ta `capabilitySetVersion()` hesaplanır, `ask()`'ta her üretim `admitArtifact()`'ten geçip `addArtifact()`'e bu alanlarla yazılır). 5 test geçti. **Canlı kanıt:** telefonda iki gerçek üretim ("Çalışan Servisler", "Pil Durumu") sorunsuz kabul edildi, `crypto.subtle` gerçek tarayıcıda patlamadı, W1'in bağımsız risk kapısı (`script.run` → ask reddi) regresyonsuz çalışmaya devam etti
      · **Kalan iş — `approvalScope` kısmı:** henüz yok. **Onay kaydı W6.L'nin hash'ine kilitlensin:** `approvalScope`, `{structureHash, capabilitySetVersion}` ikilisine bağlı saklanır — capability sürümü değişince eski onay **otomatik geçersiz** sayılır. Bu, KARAR-2'nin Katman B (sandbox/serbest kod) onay akışına bağlı — bugün üretilenlerin hepsi Katman A (risk:safe render), henüz yeni capability isteyen bir onay senaryosu **yok**; W6.4'te Katman B gelince gerçek kullanım örneğiyle kurulacak
- [x] **M-6** ✅ **FACT — tanım düzeyinde kapandı (2026-08-18).** `CanCompose(A,B,Γ,Π) = S∧C∧Π∧L∧V` formülü `MIMARI_TEMEL.md §5.1`'de zaten yazılı (aynı gün erken saatte düzeltilmişti). **Kod olarak sarmalanmadı — bilinçli erteleme:** bugün AIOS'ta bileştirilecek gerçek bir A/B (widget-widget kompozisyonu) yok, hiçbir artefakt başka bir artefaktla birleşmiyor. Boş bir `CanCompose()` fonksiyonu yazmak, W6.5d'de reddedilen erken mühendisliğin aynısı olurdu (K10). **Tetikleyici:** W6'da gerçek bir kompozisyon senaryosu (örn. iki widget'ın aynı pencerede veri paylaşması, W6.N'nin pub/sub sorusu) ortaya çıkınca formül koda geçirilir
- [x] **M-7 (owner katkısı, 2026-08-17)** ✅ **FACT — minimum dilimi kapandı.** **Sözleşme yalnızca alan değil, kapı olsun.** `admitArtifact()` (`artifact-contract.js`) galeriye yazılmadan **önce** çalışan gerçek bir kapı: kullanılan capability'leri çıkarır (`usedActionTypes()` — W6.W minimal closure), bilinmeyen referansı reddeder, sürüm damgalar. Reddedilen `addArtifact()`'e **hiç ulaşmıyor** — mevcut "reddedilen şema" UX'iyle (aynı hata kartı, aynı "İş ekleyerek tekrar üret" eylemi) birleştirildi, ikinci bir kullanıcı deneyimi icat edilmedi
      · **Dürüst sınır:** bugün pratikte çoğunlukla **savunma katmanları arası tekrar** (defense in depth) — `renderer.js`'in `validateScreen()`'i zaten bilinmeyen action'ları render'dan önce siliyor (W5), yani `admitArtifact()`'e ulaşan içerik genelde zaten temiz geliyor. Asıl yeni değer kapıda değil **damgalamada**: `capabilities`/`version`/`provenance` artık her kayıtta var (M-5). Kapının gerçek sınama anı Katman B (serbest kod, W6.4) gelince olacak — orada `validateScreen` gibi bir ön-temizlik yok, `admitArtifact` tek savunma hattı olacak
      · Ağır bir "compiler" kurulmadı (owner'ın açık talimatı) — DAG/terfi yaşam döngüsü/maliyet-tabanlı reuse **yok**, W6.5d'nin erteleme kararı korundu
- [~] **M-8 (owner katkısı, 2026-08-17)** **Geçici yürütme ↔ kalıcı artefakt iki ayrı aşama olsun.**
      · **En ucuz hali FACT:** M-7'nin kapısı zaten bunu sağlıyor — `admitArtifact()` başarısız olursa `addArtifact()` **çağrılmıyor**, hiçbir yere yazılmıyor (localStorage'a da, sunucu yedeğine de). "Doğrulanmadan kalıcılaşmaz" invaryantı bugün gerçek
      · **Kalan iş — tam kapsam:** bu **ölçülen tekrar kullanıma dayalı terfi değil** (`geçici çalıştırma → ölçüm → terfi`), yalnızca `geçerli/geçersiz → hemen kalıcı ya da hiç` ikili anahtarı. W6.F'nin IndexedDB'ye taşınması ve gerçek "önce dene, sonra kalıcılaştır" akışı hâlâ yok — bilinçli olarak ertelendi, W6.F'nin kendi işi
- [~] **M-9 (owner katkısı, 2026-08-18)** **Depolama yönü ters çevrildi: sunucu birincil, istemci önbellek.** Bugüne kadar (W6.F) `IndexedDB` istemci tarafında birincil kaynaktı, sunucudaki `/artifacts` yalnızca tek yönlü yedekti. Ters çevrildi — Fabric zaten Termux'un kendi süreci, sınırsız erişimli dosya sistemi var.
      · **TEST-VERIFIED (2026-08-18):** `loadArtifacts()` artık açılışta önce `GET /artifacts` deniyor (4sn zaman aşımı); sunucu doluysa onu kullanıp `IndexedDB`'yi onunla senkronluyor; sunucu boş/erişilemezse `IndexedDB`→eski `localStorage` sırasıyla yerel veriye düşüyor, veri varsa sunucuyu **besliyor** (böylece sunucu gerçekten birincil olur). `saveArtifacts()` önce sunucuya yazıyor, sonra önbelleği güncelliyor. `boot()`'taki tekrarlanan eski senkron çağrısı temizlendi. Build+test geçti (26/26), telefona dağıtıldı (md5 birebir 42 dosya), **sunucu tarafı** doğrulandı (`GET /artifacts` → 8 gerçek kayıt, istemci açılışta bunu bulacak)
      · **Kalan iş — FACT'e çıkması için:** istemcinin gerçekten sunucudan okuduğu tek bir sayfa açılışıyla canlı kanıtlanmalı (K5) — henüz yapılmadı
      · Kapsam: yalnızca depolama katmanı — Fabric'in kendisi Termux'ta kalır, Ubuntu'ya taşınmaz (B-11)

## Bilinen borçlar (henüz sıraya girmedi)

- [x] B-16 ✅ **KAPANDI (2026-08-20, FACT)** — premium UX audit ilk bulgusu: `public/sw.js` `SHELL_FILES` precache listesi 30 `public/js` dosyasının yalnız 5'ini taşıyordu (`formation-explorer.js`, `windowmanager.js`, `application-model.js` dahil 25 modül eksikti). `app.js` neredeyse her modülü üstte statik import ettiği için ilk online oturum tamamlanmadan (kurulum kesintiye uğrar veya hemen ardından uçak modu açılırsa) offline'a geçen kullanıcı boş/ölü ekranla kalırdı. `SHELL_FILES` gerçek dizin içeriğine genişletildi, `SHELL` `v6→v7`. `test/sw-shell.test.ts` (registry-drift.test.ts deseniyle) eski listeyi önce düşürdü (2/5 fail), yeni listede 5/5 geçti; tam suite 179/179, `BUILD_OK`. Telefonda `GET /sw.js` → `aios-shell-v7`, listedeki 33 varlığın tamamı `200` (caches.addAll atomik olduğu için kritik), md5 depo==telefon (108 dosya). Ayrıntı: `PRODUCT_GAP_REGISTER.md` `PG-024`.

- [x] B-14 ✅ **KAPANDI (2026-08-20, FACT)** **(aynı gün canlı reuse denemesinde bulundu)** Runtime Ledger yazıcı/okuyucu sözleşme sapması — `scripts/aios-runtime-ledger.sh` `status=missing` satırında `commandHash` ve `processWitness` alanlarına `-` yazarken `src/runtime-provenance.ts:68-72` (`verifyRuntimeLedgerText`) bu iki alan için koşulsuz 64-hex hash istiyor. `verifyRuntimeLedgerText` dosyanın tamamını önce doğruladığı için **2026-08-20T00:42:42Z'den sonraki her provenance yazımı fail-closed**; `task.completed → RuntimeWitness → immutable edge` zinciri yeni edge üretmiyor. Ledger append-only + hash-zincirli olduğundan kendiliğinden düzelmez.
      · **Bağımsız doğrulandı:** telefondaki 145 satırın yalnız 2'si (127, 128) reddediliyor, ikisi de `status=missing`; zincir bütünlüğü ve TÜM event-hash'ler geçerli, `aios-runtime-ledger.sh verify` `LEDGER_OK` diyor. Sorun zincir bütünlüğü DEĞİL, okuyucunun alan regexi.
      · **Owner kararı (2026-08-20):** üretim provenance çekirdeğine bu turda dokunulmadı, yazıcı sözleşmesi değiştirilmedi, geçmiş satırlar yeniden yazılmadı. Exact invariant farkı + `T1-T6` kabul testi `PRODUCT_GAP_REGISTER.md` `PG-022` içinde tanımlı; düzeltme owner onayına bağlı.
      · **DÜZELTİLDİ (2026-08-20, owner onaylı dar kapsam):** `verifyRuntimeLedgerText` alan doğrulaması yazıcının gerçek sözleşmesine hizalandı — `commandHash`/`processWitness` için `-` YALNIZ `status="missing"` satırında geçerli. `checkpointFrom()`, `previousHash` zinciri, `eventHash` SHA-256 doğrulaması ve yazıcı sözleşmesi DOKUNULMADI; `src/` altında yalnız `runtime-provenance.ts` değişti (+15/-2).
      · **Testler önce ısırdı sonra yeşil:** T1-T6 düzeltmeden önce 5 düşüyordu (T1/T3/T4/T5/T6; T2 zaten geçiyordu çünkü reddi doğruluyor), sonra 19/19. T5 gevşemenin hash kapısını açmadığını kanıtlar. T6 için gerçek 145 satırlık ledger `test/fixtures/runtime-ledger-2026-08-20.tsv` olarak donduruldu.
      · **FACT — canlı kanıt:** yerel+telefon `npm test` 174/174, `BUILD_OK`, md5 depo==telefon (107 dosya). Gerçek `volume.set` → formation **22 → 22**, provenance edge **15 → 16** (`provenance-edge:92f10d96…`, parent exact Ses Paneli `formation:53542f33…c030c655`, witness `runtime-witness:9562590f…`, task `6fa9b709-…`), `LEDGER_OK events=151`, geçmiş **145 satır byte eşit** (yalnız append). İkinci bağımsız reuse **16 → 17**.
- [ ] B-15 **(2026-08-20)** `~/.shortcuts/watchdog.sh` kanoniklik kapsamı dışında — `deploy-to-phone.sh` yalnız `~/watchdog.sh`'i md5 doğrular; `.shortcuts` kopyası (md5 `1130a903…`) kanonikten geride ve Runtime Ledger `observe watchdog-cycle` bloğunu taşımıyor. Widget'tan başlatılırsa `/runtime-status` ve `start_hermes_os.sh` sağlık satırı watchdog'u "down" gösterir (desen `^bash $HOME/watchdog\.sh$`) ve ledger sürekliliği beslenmez. Ayrıntı: `PRODUCT_GAP_REGISTER.md` `PG-023`. Owner kararı olmadan başlatıcı matrisine dokunulmadı.

- [x] B-1 `pc-agent` Agent Card sürümü `0.3.0`, `package.json` `0.1.0` — W2.2 ile kapandı, ikisi de artık `0.1.0`
- [x] B-2 `a2a.ts:pollPeerTask()` ölü kod — W2 sırasında silindi
- [x] B-3 ✅ **KAPANDI (2026-08-18)** — `fabric/public/js/components.css` **silindi**. Doğrulama: `aios.html` yalnızca `/css/components.css`'i yüklüyor; `js/` kopyasına sıfır referans; ve artık birebir kopya bile değildi (W6.I'nin native toast/sheet CSS'i yalnızca `css/` sürümüne eklenmişti, ölü kopya hiç güncellenmemişti — bu da terk edildiğinin ayrı bir kanıtı)
- [ ] B-4 `vendor/` + `icons/` md5 doğrulama kapsamı dışında (nadiren değişir, bilinçli)
- [x] B-5 ✅ **KAPANDI (2026-08-18)** — çalışan PC agent (PID doğrulandı) kendi Agent Card'ında zaten kanonik depoyu (`aios-fabric`) `SAFE_ROOT` olarak bildiriyordu (curl ile doğrulandı). Eski oturum klasöründeki (`Telefon_AI_Agent_Session_2026-08-16/pc-agent`) kullanılmayan kopya `D:\projects-archive\reference\pc-agent-session-2026-08-16\`'ya taşındı (silinmedi) — aynı klasördeki SSH anahtarlarına (`keys/`) ve diğer oturum dosyalarına dokunulmadı
- [x] B-6 ✅ **KAPANDI (2026-08-17)** — MCP'deki desenin aynısı: **test kapısı** ile bağlandı, tek kaynağa geçirilmedi (o daha büyük bir refactor, W6 sırasında değerlendirilebilir; öncelik olan drift riskiydi, o kapandı)
      · `screenspec.ts`'teki `ALLOWED_TYPES`/`UI_META_ACTIONS` **export edildi**; istemci tarafındaki eşdeğeri `app.js` içinden bağımsız bir modüle (`public/js/ui-actions.js`) çıkarıldı — çünkü `app.js` top-level'da `window`'a bağlı (satır 766), Node testi onu doğrudan import edemezdi
      · Yeni `fabric/test/registry-drift.test.ts`: iki tip listesini (19 bileşen) ve iki eylem listesini (15 `ui.*`/`cap.test`) karşılaştırır
      · **FACT — kanıtlandı iki yönlü:** (a) listeye sahte bir eleman eklenip testin **patladığı** görüldü, geri alınca 2/2 geçti — test gerçekten drift'i yakalıyor; (b) canlı `:9300` `200`, `/js/ui-actions.js` `200`, telefonda 14/14 test (12 eski + 2 yeni), md5 birebir (36 dosya)
- [x] B-8 ✅ **KAPANDI (2026-08-18)** — `deploy-to-phone.sh`'in 3. adımı artık `src/`+`js/`+`css/` içinde yerelde olmayıp telefonda kalan dosyaları **gerçekten siliyor** (önceden yalnızca `src/*.ts` için uyarıyordu, silmiyordu).
      · **Canlı kanıt, iki denemeyle (2026-08-18):** telefona sahte bir dosya konup deploy çalıştırıldı — **ilk düzeltme kendisi hatalıydı**: uzak `rm` komutunda `'~/fabric/...'` tek tırnak içinde yazılmıştı, uzak shell tırnak içindeki `~`'yi genişletmiyor, `rm -f` sessizce hiçbir şey silmiyordu (`exit 0`, etkisiz) — `ls` ile dosyanın hâlâ durduğu görülüp yakalandı. Tırnaklar kaldırılıp ikinci kez test edildi: sahte dosya **gerçekten silindi**, `ls` "No such file" döndü, `deploy --check` temiz (42 dosya)
      · `vendor/`/`icons/` kasıtlı olarak dokunulmadı (B-4, nadiren değişir)
- [x] B-7 ✅ **KAPANDI (2026-08-18)** — A2A idempotency artık gerçek uçtan-uca. `A2ATask`'a `messageId` alanı eklendi, `A2AHub`'a `messageIdToTask` haritası (journal'dan replay'de de yeniden kurulur). `createInboundTask(text, contextId, messageId)`: aynı `messageId` ile ikinci çağrı **yeni görev açmıyor**, var olanı doğrudan dönüyor — capability ikinci kez çalışmıyor. `server.ts`'teki JSON-RPC `SendMessage` yolu artık `body.params.message.messageId`'yi taşıyor
      · 4 yeni test (`a2a-idempotency.test.ts`): aynı id→aynı görev, farklı id→farklı görev, id yoksa eski davranış (geriye uyumlu), dedup edilen görevin gerçekten tamamlanmış haliyle döndüğü
      · **Canlı kanıt, gerçek cihazda:** aynı `messageId` ile iki gerçek `SendMessage` isteği → **aynı `task.id`** (`3d4ec13f-...`); farklı `messageId` → farklı `task.id` (`1b4da04f-...`) — dedup ne eksik ne fazla agresif
- [~] B-9 **(2026-08-17'de bulundu, W6.C doğrulaması sırasında)** Android arka planda **tüm Termux oturumunu** (`llm_bridge` + `watchdog.sh` + eski `fabric`) öldürebiliyor — **2026-08-17'de iki kez**, **2026-08-18'de dört kez** (üçüncü: SSH portu 8022 tamamen kapandı, `sshd` dahil tüm Termux gitmiş; dördüncü: bu oturumda, M-9 canlı testinin hemen ardından — SSH+HTTP ikisi de tamamen erişilemez oldu, dört sürecin dördü de ölmüştü, `start_hermes_os.sh` ile yeniden başlatıldı) — **toplam altı kez, iki oturumda**. `termux-wake-lock` yeterli gelmiyor.
      · **KÖK NEDEN BULUNDU (2026-08-18, K8 disipliniyle koda/cihaza soruldu, tahmin edilmedi):** bugünkü dört kesinti **cihaz REBOOT'u DEĞİL** — kanıt: `~/.termux/boot/aios-boot.log`'da tek giriş var (`Mon Aug 17 07:55:52`, dünkü ilk kurulum), `uptime` bugün `1 day, 2:17` gösteriyor. Yani `Termux:Boot`/`10-aios.sh` (zaten kurulu, önceki oturumda yazılmış) bugünkü kesintilerle **hiç ilgili değildi** — `BOOT_COMPLETED` hiç tetiklenmedi. Gerçek neden: **MIUI/HyperOS (Xiaomi, Android 15, build V816)** cihaz açıkken Termux sürecinin tamamını arka planda öldürüyor — bu bir kod sorunu değil, cihaz ayarı sorunu (persist()'in SELinux sınırıyla aynı sınıf: kod tarafında düzeltilemez, owner'ın fiziksel/ayar müdahalesi gerekiyor)
      · **Owner'a verilen somut adımlar (Xiaomi/MIUI standart):** (1) Güvenlik uygulaması → İzinler → Otomatik başlatma → Termux açık, (2) Ayarlar → Uygulamalar → Termux → Pil → "Kısıtlama yok", (3) Son uygulamalar ekranında Termux kartını kilitle (LMK/bellek temizliğinden muaf tutar)
      · **Ayrı bir drift bulundu ve kapandı:** `scripts/watchdog.sh` (repo) telefondaki canlı sürümden GERİDEYDİ (2026-08-16 ankraj düzeltmesi repoya hiç yansımamıştı) — telefondan çekilip kanonikleştirildi
      · **TEST-VERIFIED → FACT (2026-08-18, canlı kanıtlandı):** watchdog döngüsü SADECE `fabric`+`llm_bridge`'i izliyordu, **`hermes gateway` (8642, A2A) hiç izlenmiyordu** — tek başına ölürse bir sonraki manuel `start_hermes_os.sh`'e kadar geri gelmiyordu. Üçüncü kontrol bloğu eklendi (aynı desen/stil). **Canlı kanıt:** `hermes gateway` süreci kasıtlı `pkill -9` ile öldürüldü, watchdog'un 45sn'lik döngüsünde otomatik geri geldiği hem `pgrep` hem `watchdog.log`'daki yeni satırla (`Tue Aug 18 10:16:42: hermes gateway down, restarting`) doğrulandı
       · **Kalan iş:** MIUI ayar değişiklikleri owner tarafından yapılacak (kod tarafında yapılabilecek başka bir şey yok — `watchdog.sh` artık üç süreci de izliyor ama OS düzeyinde `watchdog.sh`'in KENDİSİ öldürülürse bu üçü de savunmasız kalır, "gözcünün gözcüsü yok" sorunu MIUI ayarları düzelmeden çözülemez)
       · **FACT — anlık canlı sağlık (2026-08-18, 13:25 +03):** `fabric`, `llm_bridge`, Hermes gateway ve watchdog süreçleri mevcuttu; Fabric HTTP 200, üç servis portu (`9300`/`9201`/`8642`) yerelde açıktı. Bu yalnız o anın sağlık kanıtıdır; Android/MIUI'nin Termux'u yeniden öldürmeyeceğini kanıtlamaz.
- [x] B-12 ✅ **FACT (2026-08-18) — canlı kanıtlandı.** **(2026-08-18'de bulundu, 10-artefakt canlı testi sırasında)** Veri döndüren `REFLEX` capability'lerin (`sensor.location.read`, `app.list`, `wifi.info`, `volume.read` vb.) sonucu, HERMES sohbeti dışında (örn. bir ARTEFAKT kartındaki butondan) tetiklendiğinde **hiçbir yerde görünmüyor**. Kök neden koda soruldu, tahmin değil: `app.js:ctx.dispatch()` (satır 191-279) yalnızca `script.run` için özel bir gösterim koduna sahip (satır 260-269, sonucu `chat.push` ile HERMES'e yazıyor); geri kalan **her** capability için sonuç yalnızca `lastReceipt`'e (satır 272-276) yazılıyor — bu değişken de yalnızca bir sonraki `ask()` çağrısı içinde (satır 803/854) HERMES sohbetine düşüyor. **Canlı kanıt:** journal'da hem `sensor.location.read` (`sensor.read.confirmed`: gerçek GPS koordinatı döndü) hem `app.list` (`task.completed`: gerçek uygulama listesi döndü) **başarıyla tamamlandı** — capability'ler çalışıyor, yalnızca UI göstermiyor. Owner'ın canlı test raporu ("Konum Bilgisi bir şey vermedi", "Uygulama Listesi işe yaramadı") bu yüzden — capability hatası değil, gösterim boşluğu
      · **TEST-VERIFIED (2026-08-18) — düzeltildi.** `public/js/dispatch-utils.js:hasMeaningfulData()` yazıldı (saf, DOM'suz — `windowmanager.js`/`artifact-parse.js` ile aynı desen, Node'da doğrudan test edilebilir): salt yan-etkili capability'ler (`torch.set`/`vibrate`/`wakelock.*`) boş stdout döndürür (`data:""`), veri-döndüren capability'ler gerçek içerikli JSON döndürür — bu ayrım "gösterilecek bir şey var mı" sorusuna deterministik cevap veriyor. `ctx.dispatch()`'e `script.run`'ın **kendi** deseni (action-receipt + mono metin, HERMES'e düşme, tab dışındaysa toast) `else if (res.ok && hasMeaningfulData(res.data))` dalıyla **genellendi** — yeni bir gösterim biçimi icat edilmedi (K10)
      · **5 yeni test** (`dispatch-utils.test.ts`, 54/54 toplam geçti): boş stdout/null/undefined → false, boş obje/dizi → false, gerçek içerikli obje (konum) → true, gerçek içerikli dizi (uygulama listesi) → true, JSON olmayan dolu metin (örn. SSID) → true
      · **Canlı kanıt (K5, owner, 2026-08-18):** "Konum Bilgisi" ve "Uygulama Listesi" test artefaktlarının butonlarına tekrar basıldı, ikisi de HERMES'te gerçek sonuçla göründü — regresyon yok, boşluk kapandı
- [x] B-13 **KAPANDI (2026-08-18, Approval Contract).** `risk:ask` capability'ler artık kalıcı onay kaydına bağlı çalışıyor — koşulsuz red kalktı. `src/approval.ts` (saf `isApproved(state, capability, now)`), `src/types.ts` (`ApprovalRecord`/`FabricState.approvals`), `src/state.ts` (`approval.granted/denied/revoked` reducer case'leri, journal'daki mevcut append-only desenin AYNISI), `src/dispatcher.ts` (`grantApproval/denyApproval/revokeApproval` — **capabilityMap'te YOK**, yani MCP `tools/call`/A2A/otomasyon `dispatch()` dışına çıkamadığı için approval'i asla kendileri veremez; yalnızca insan-tetikli `server.ts` uçları çağırabilir: `POST /approvals/grant|deny|revoke`, `GET /approvals`). Scope bugün **capability-düzeyinde** (KARAR-2'nin "kapsam" kısmı) — artefakt/capability-set düzeyi (`capabilitySetVersion` ile invalidation) Katman B/DAG gelmeden **TARGET** olarak bırakıldı, sahte test yazılmadı. Onay UI'sı (Control Center paneli) bilinçli olarak bu turun DIŞINDA — owner'ın seçimiyle yalnızca contract+test yapıldı. 10 yeni Node testi (`test/approval.test.ts`, `npm test` 64/64). **Canlı kanıt (K5, telefon, gerçek `script.run`+bash):** onaysız `POST /intent {script.run}` → `task.failed` (`"onay gerektirir..."`) → `POST /approvals/grant` → aynı capability tekrar dispatch → `task.completed`, gerçek `stdout` döndü ("B13_LIVE_TEST_2" = 15 karakter eşleşti) → `POST /approvals/revoke` → tekrar `task.failed`. `GET /approvals` denetim kaydını doğru gösterdi. **FACT.**

  **Onay UI'sı da eklendi ve KAPANDI (aynı gün, ikinci dilim).** Control Center'a "İZİNLER" bölümü eklendi (`public/js/app.js:openControlCenter`) — HAM KAYIT (M-10) ile AYNI desen: düz `fetch`/`postJSON` ile `/approvals`, `/approvals/grant`, `/approvals/revoke`'e bağlı, capability DEĞİL. Boot'ta yüklenen `capabilitiesWithRisk`'ten `risk==="ask"` olan capability'ler listelenir, her satırda chip (ONAYLI/REDDEDİLDİ/GERİ ALINDI/ONAY BEKLİYOR), dokununca grant↔revoke. **Canlı kanıt (K5, gerçek dokunuş, Chrome uzantısı bağlanamadığı için owner telefonda bizzat test etti):** journal'da `approval.granted{script.run}` (08:45:30.671) → `approval.revoked{script.run}` (08:45:32.705), ~2sn arayla — SSH ile journal'dan (`fabric-journal.db`, doğrudan sorgu) doğrulandı, curl testlerinden (08:16) tamamen ayrı bir zaman damgası. Client-error log'da hata yok. **FACT.**
       · **Enforcement uzantısı FACT (2026-08-18, telefon):** `/read` yalnız `risk:"safe" + readOnly:true` olan `sensor.battery.read` ve `wifi.info` için 200 verdi; `torch.set`, `sensor.location.read`, `script.run` 403 fail-closed oldu. Bu endpoint genel execution değildir; reddedilen sentinel isteğinde journal'a yeni task/read-failed olayı düşmedi.
       · **A2A approval uyumu FACT (2026-08-18, telefon):** onaysız `script.run` A2A görevi ve dispatcher görevi reddedildi; insan `POST /approvals/grant` sonrası aynı A2A isteği dispatcher üzerinden gerçek stdout ile tamamlandı; revoke ve geçmiş `expiresAt` sonrası tekrar reddedildi. A2A'nın `approval.granted` isteği bilinmeyen capability olarak reddedildi; approval grant edemedi.
- [x] **M-10 (owner katkısı, 2026-08-18)** ✅ **FACT — Ham prompt/yanıt kaydı, sigortalı anahtar.** DeepSeek Harness'ın "Trajectory" özelliği tartışılırken ortaya çıktı: journal bugün yalnızca **kontrol düzlemini** tam tutuyor (hangi capability, ne zaman, sonuç — W5'in tek-bus'u), **içerik düzlemini** (ham LLM prompt/yanıtı, paylaşılan metin) W1.7'den beri bilinçli redakte ediyordu (`dispatcher.ts:redactFields`/`sensitiveResult`). Owner ham içeriği de istedi ama **sigortalı** — istediği an aç/kapa. Yapıldı: `fabric/src/debugtrajectory.ts` (dosya-varlığı bayrağı, `appicons.ts:NET_FLAG` ile aynı desen), `dispatcher.ts`'te hem girdi hem çıktı redaksiyonu bu bayrağa bağlandı, `server.ts`'e `/debug-trajectory` GET/POST eklendi, Control Center'a **"HAM KAYIT"** anahtarı kondu
      · **Kasıtlı güvenlik sınırı:** bu anahtar **capability DEĞİL** — `capabilityMap`'e hiç girmiyor, yalnızca insan arayüzünden değiştirilebilir. LLM/A2A/MCP kendi kararıyla gerçek kullanıcı mesajlarının kalıcı loglanmasını **asla açamaz** (W1'in "onay katmanına ajan dokunmaz" ilkesinin uzantısı)
      · **Canlı kanıt, dört yönlü (2026-08-18):** anahtar açıkken gönderilen benzersiz test ifadesi hem `task.created` (girdi) hem `task.completed` (çıktı) olaylarında **ham hâliyle** journal'da görüldü; anahtar kapatılınca **aynı oturumda**, bir sonraki istekte redaksiyon (`"24 karakter"`, `"redacted":true`) anında geri döndü
- [ ] B-11 **(2026-08-18'de bulundu, mimari tartışması sırasında)** **Fabric'i Ubuntu proot-distro'ya taşıma fikri değerlendirildi, gerçek bir engelle karşılaşıldı.** `start_hermes_os.sh`'nin kendi yorumu (`# Termux'un KENDI Node'unda: capability'ler Termux:API binary'lerini ve am/pm'i dogrudan cagiriyor (proot'ta rish/izinler bozuluyor)`) daha önce test edilip belgelenmiş: `capabilities.ts`'te **18+ capability** (`torch.set`, `vibrate`, `clipboard.*`, `notification.send`, `tts.speak`, `speech.listen`, `sensor.battery.read`, `wifi.info`, `volume.*`, `brightness.set`, `app.open`/`link.open` (`am start`), `app.freeze`/`unfreeze` (`pm` via rish)) doğrudan `termux-*` ikilileri veya `am`/`pm`'e bağlı — Termux:API'nin kendi Android IPC'sine dayanıyor, proot'un syscall çevirisinde bozulması **beklenir** (kanıtlanmış, yeniden test edilmedi). **Karar: Fabric Termux'ta kalır**, yalnızca depolama katmanı (M-9) sunucu tarafına kayar — Hermes/`llm_bridge` zaten Ubuntu'da, bu değişmiyor
- [x] B-10 ✅ **KAPANDI (2026-08-17)** — Uygulama tamamen kapatılıp yeniden açıldığında soğuk başlangıçta `#tabbar` görünmüyordu, klavye aç/kapat düzeltiyordu. **Kök neden koda bakılarak çıkarıldı:** `aios.html`'deki `sync()` sayfa yüklenir yüklenmez **senkron** çağrılıyordu (`vv.addEventListener(...); sync();`); soğuk PWA açılışında Android'in sistem çubukları henüz yerleşmeden `window.innerHeight` ile `visualViewport.height` geçici olarak uyuşmuyor, `kb = innerHeight - vv.height - vv.offsetTop` yanlışlıkla `120`'yi aşıp `body.kb` → `#tabbar{display:none}` tetikliyordu. Klavye aç/kapat düzeltiyordu çünkü gerçek bir `resize` olayı, değerler zaten oturmuşken tetikleniyor. **Düzeltme:** ilk çağrı çift `requestAnimationFrame` ile iki çizim karesi ertelendi, değerler oturduktan sonra hesaplanıyor. **Canlı kanıt:** owner uygulamayı tamamen kapatıp (yalnızca sekme değil) yeniden açtı — *"evet sekmeler geliyor"*
