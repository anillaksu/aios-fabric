# OTURUM KAYDI — 2026-08-18

**Bu dosya bu oturumun detaylı özetidir.** Güncel iş listesi her zaman
`docs/CHECKLIST.md`'dir — burası "ne olduğunu" anlatır, oradaki "şu an ne
durumda"yı gösterir. Context %89'da kaydedildi, sonraki oturum için.

Önceki oturum kaydı: `docs/OTURUM_2026-08-17.md` (context %84'te kaydedilmişti,
W0-W5 + W6 kararları). Bu dosya doğrudan onun devamı.

---

## 1. Bu oturumda ne oldu (kronolojik, önemli olanlar)

1. **`docs/MIMARI_TEMEL.md` yazıldı** — owner'ın mimari değerlendirme raporu
   K8 disipliniyle koda soruldu, 8 düzeltme çıktı. En önemlisi: **AETHER
   yürütme katmanı değil** (execute:false, sıfır import, offline-first) —
   yönetişim/politika katmanı, `dispatcher.dispatch()` yürütmenin tek sahibi.
2. **Dört seviyeli kanıt skalası** kuruldu (`FACT › TEST-VERIFIED ›
   REVIEW-VERIFIED › TARGET`) — üç seviyeli eskisinden daha isabetli, bu
   oturumda defalarca (W6.I, W6.B/C, W6.D, W6.L) doğru çalıştığı kanıtlandı.
3. **W6.I** — Framework7 (1.27 MB) atıldı, native `<dialog>`/Popover ile
   değiştirildi. Canlı görsel kanıt: Control Center paneli açıldı.
4. **B-6 kapandı** — `screenspec.ts`/`registry.js` arasındaki drift riski
   test kapısıyla (MCP'deki desenin aynısı) kapatıldı.
5. **Spec düzeltmesi** — `PLAN_W6_app-shell.md`'nin kendi içinde çelişkisi
   bulundu (W6.2 eski masaüstü modelini tarif ediyordu, KARAR-1 bunu
   reddetmişti) — kod yazılmadan önce düzeltildi.
6. **W6.B/C (WindowManager + gerçek artefakt lifecycle)** — yüzeyden bağımsız
   çekirdek yazıldı, gerçek bir artefaktla (Hermes'in ürettiği) açılış-kapanış
   döngüsü canlı kanıtlandı. Yol boyunca gerçek bir regresyon bulunup
   düzeltildi (composer'ın otomatik KOMUT'a geçişi `fillTarget`'ı temizliyordu).
7. **M-5/M-7/M-8 (Artifact Contract kapısı)** — owner'ın net yönlendirmesiyle
   ("ağır compiler değil, minimum sınır") `admitArtifact()`/`usedActionTypes()`
   kuruldu, canlı iki gerçek üretimle kanıtlandı.
8. **W6.C orijinal kapsam (boş pencere akışı)** — "Yeni boş pencere" +
   Hermes'in yalnızca içini üretmesi. Gerçek bir regresyon bulunup düzeltildi.
9. **W6.F (IndexedDB göçü)** — `localStorage`'dan `IndexedDB`'ye, sıfır veri
   kaybıyla canlı kanıtlandı (gerçek kullanıcı verisi taşındı, önce sunucu
   yedeği alındı).
10. **B-10 kapandı** — soğuk açılışta sekmelerin görünmemesi, kod okumasıyla
    kök nedeni çıkarıldı (`visualViewport` senkron çağrısının zamanlama
    yarışı), çift `requestAnimationFrame` ile düzeltildi, canlı kanıtlandı.
11. **`persist()` doğrulaması sonuna kadar kovalandı** — SELinux/Shizuku/root
    araştırması üç katmanlı kanıtla tükendi (bkz. §3). Owner'ın sert
    geri bildirimi: *"erişimin olmayan bir şey yok, kontrol etmiyorsun"* —
    gerçekten denendi, gerçek bir OS sınırına (üretim derlemesinde SELinux)
    çarpıldığı kanıtlandı.
12. **Mimari tartışma: M-9 + B-11** — owner'ın sorusu ("Hermes zaten Termux'ta
    özgür, neden Chrome IndexedDB'yle uğraşıyoruz") depolama yönünü tersine
    çevirdi: sunucu birincil, IndexedDB yalnızca önbellek. Fabric'i Ubuntu'ya
    taşıma fikri de değerlendirildi, gerçek bir engelle (18+ capability
    `termux-api`/`am`/`pm`'e bağlı, proot'ta bozuluyor) reddedildi.
13. **M-10 — ham prompt/yanıt kaydı, sigortalı anahtar.** DeepSeek Harness
    tartışması sırasında ortaya çıktı. Control Center'da **"HAM KAYIT"**
    anahtarı — capability DEĞİL, yalnızca insan arayüzünden açılır/kapanır.
14. **B-5, B-7, B-8, M-6, S-6, W6.H, W6.W kapandı** (otonom çalışma turu).
15. **W6.L (prompt→artefakt önbelleği)** — owner'ın scope kararı (yalnızca
    güvenli canonicalization, dolgu kelimesi silme yok) ve güvenlik uyarısı
    (anahtar dört bileşenli: prompt+capability-risk+registry+model) ile
    kuruldu. **Kanıt yolculuğu önemli:** ilk iki canlı denemede "bekledi"
    hatası alındı — gerçek neden önbellek değildi, **B-9 üçüncü kez tekrarladı**
    (`llm_bridge` öldü). Servis yeniden başlatılıp üçüncü denemede FACT'e çıktı.
16. **Sessiz catch denetimi (owner talimatı: "hepsini kabul edilemez")** —
    tüm proje tarandı, 34 sessiz hata-yutan `catch` bloğu bulundu, 32'si
    düzeltildi (2'si gerekçeli istisna). Yeni `client-log.js` (istemci,
    hem konsol hem `/client-error`→journal) ve `log.ts` (sunucu, `fabric.log`).

## 2. Şu an durum

- **Depo:** `C:\Users\anil\Desktop\aios-fabric`, `git status` temiz, son commit `8d9e8f5`
- **Telefon:** `deploy-to-phone.sh --check` → depo ile birebir aynı (**46 dosya**)
- **Testler:** `fabric/test/*.test.ts` — **37/37** geçiyor, her dağıtımda otomatik
- **Checklist:** **80 tamamlanan / 34 kalan / 7 kısmi** (`docs/CHECKLIST.md`)
- **W6 ilerlemesi:** I, T, B, C (iki dilim), D (bulgu), F, H, L, W kapandı/FACT;
  M-5..M-10 çoğunlukla kapandı; kalan çoğu madde (G, K, N, O, P, R, S, U, V, Y, Z)
  ya gerçek ürün kararı gerektiriyor ya da Katman B'ye (henüz yok) bağımlı

### ⚠️ En önemli açık risk: B-9 (bugün ÜÇ kez tekrarladı)

Android arka planda Termux oturumunu (`llm_bridge` + `watchdog.sh` + bazen
`sshd` dahil her şeyi) öldürüyor. Bu oturumda üç kez oldu, ikisi doğrudan
doğrulama sürecini yanılttı (W6.L'de "önbellek bozuk" sanıldı, gerçekte
backend ölmüştü). **Sonraki oturuma başlarken önce bunu kontrol et:**

```
KEY="$HOME/Desktop/Telefon_AI_Agent_Session_2026-08-16/keys/phone_termux_key"
ssh -p 8022 -i "$KEY" u0_a322@100.75.177.88 'echo CANLI'
```

Erişilemezse önce owner'dan Termux'u açıp `sshd` başlatmasını isteyin.
Erişim varsa `ps -ef | grep -E "uvicorn|hermes gateway|node.*server.ts|watchdog"`
ile dört sürecin de ayakta olduğunu doğrulayın; biri eksikse
`bash ~/start_hermes_os.sh` (arka planda, `nohup ... & disown`) ile
yeniden başlatın.

### Ortam bilgisi (değişmedi, önceki oturumla aynı)
- Telefon Fabric: `http://100.75.177.88:9300` (Tailscale), SSH `u0_a322@100.75.177.88:8022`
- PC agent: `http://100.109.236.30:9310` (yerelde `http://127.0.0.1:9310`), kanonik depodan çalışıyor
- Token'lar diskte kalıcı: `~/fabric/.a2a-token`, `~/fabric/.mcp-token` (telefon), `.pc-agent-token` (PC)
- SSH key: `$HOME/Desktop/Telefon_AI_Agent_Session_2026-08-16/keys/phone_termux_key`
- Eski pc-agent kopyası artık `D:\projects-archive\reference\pc-agent-session-2026-08-16\`'da (B-5)

## 3. `persist()` — kovalandı, kanıtla tükendi (TARGET kaldı ama nedeni net)

Owner Kablosuz Hata Ayıklama'yı açtı, gerçek bir Shizuku oturumu kuruldu
(`rish -c id` → `uid=2000(shell)` başarıyla döndü — sunucu gerçekten çalıştı).
Chrome'un IndexedDB durumunu okumaya çalışınca kesin bir OS duvarına çarpıldı:
`getenforce` → `Enforcing`, `ro.build.type` → `user` (üretim derlemesi).
**Sonuç: Android'in SELinux politikası üretim derlemesinde `shell` UID'ini
bile başka bir uygulamanın özel verisine erişimden engelliyor.** Root
olmadan aşılamaz — Shizuku root vermiyor. Owner root'lamayı düşünüyor
(Grok'tan HyperOS 3.0/Android 15/`ziyi` codename'e özel yöntem araştırıyor),
ama bu **fiziksel USB+fastboot erişimi** gerektiriyor, uzaktan yapılamaz.

## 4. Bu oturumun kalıcı kayıtları (dosya haritası)

| Dosya | İçerik |
|---|---|
| `docs/CHECKLIST.md` | **Tek doğruluk kaynağı** — 80/121 madde, K1-K10, S-1..S-8, B-1..B-11, M-1..M-10 |
| `docs/MIMARI_TEMEL.md` | Bu oturumda yazıldı — kanıt skalası, sözleşme, birleşim, AETHER'ın yeri |
| `docs/PLAN_W6_app-shell.md` | W6 tasarımı, KARAR-1/2/3, §W6.2 bu oturumda düzeltildi |
| `docs/STANDARTLAR.md` | Değişmedi (önceki oturumdan) |
| `docs/OTURUM_2026-08-17.md` | Önceki oturum kaydı |
| `docs/OTURUM_2026-08-18.md` | Bu dosya |
| `fabric/src/log.ts`, `fabric/public/js/client-log.js` | Bu oturumda yazıldı — hata görünürlüğü |
| `fabric/public/js/windowmanager.js`, `artifact-store.js`, `artifact-contract.js`, `prompt-cache.js` | Bu oturumda yazıldı |
| `fabric/test/*.test.ts` | **37 otomatik sözleşme testi** (bu oturumda 12→37) |

AETHER'a bu oturumda atılan deltalar: `aether://project/aether-fabric` altında,
`session_id: 2026-08-17-aios-w6i-mimari-temel` ile aranabilir — hepsi
PENDING/UNVERIFIED, onay owner'da.

## 5. Bu oturumun kalıcı dersleri (kalıcı hafızaya da yazıldı)

1. **"Erişemiyorum" demeden önce gerçekten dene.** SSH/curl/dosya erişimi
   varsa kullan; sınır yazacaksan kanıtla yaz (`once-dogrulama-sonra-sinir-yaz.md`).
2. **Araştır, tahmin etme.** WebFetch/arama varken APK söküp reverse-engineer
   etmeye çalışmak yerine önce resmi kaynağa bak.
3. **Bağlamı önce oku.** Kısa/emir kipli istekler bağlamdaki özel bir ada
   atıftır (`dikkat-baglami-once-oku.md`).
4. **Sessiz hata yutan hiçbir yer kabul edilemez.** Her `catch` ya loglar
   ya da gerekçesini açıkça yazar — ikisi arasında üçüncü bir seçenek yok.

---

## 6. GERİ DÖNÜŞTE KULLANILACAK PROMPT

Aşağıdaki metni bir sonraki oturumun başında olduğu gibi yapıştırın:

```
AI-OS Fabric'e devam ediyoruz. Önce mcp__aether__aether_handoff
(aether://project/aether-fabric) çağır, sonra docs/OTURUM_2026-08-18.md
ve docs/CHECKLIST.md'yi oku. Çalışma dizini C:\Users\anil\Desktop\aios-fabric.

ÖNCE B-9'U KONTROL ET: telefon bugün üç kez arka planda öldü (SSH dahil).
SSH ile eris (docs/OTURUM_2026-08-18.md'deki key yolu), dört surecin de
(fabric, llm_bridge, hermes gateway, watchdog) ayakta oldugunu dogrula,
biri eksikse start_hermes_os.sh ile yeniden baslat. Bu adimi atlarsan
bir sonraki canli test yanlis teshis edilir (bugun iki kez oldu).

Durum: 80/121 checklist maddesi tamam. W6.I/T/B/C/D/F/H/L/W ve
M-5..M-10'un cogu, B-3/5/6/7/8/10/11 kapandi. Son is W6.L'nin (prompt
onbellegi) canli kanitlanmasiydi - basarili oldu.

Acik en onemli risk: B-9 (Android'in Termux'u oldurmesi, uc kez
tekrarladi). Kalici cozum arastirilmadi - Termux:Boot / foreground
service / pil istisnasi secenekleri var, henuz denenmedi.

persist() TARGET kaldi - SELinux/uretim-derlemesi siniri kanitlandi,
root gerekiyor, owner kendi arastiriyor (fiziksel USB erisimi gerekli,
uzaktan yapilamaz).

Kalan checklist maddelerinin cogu (W6.G/K/N/O/P/R/S/U/V/Y/Z) ya gercek
urun karari gerektiriyor (owner'a sorulmadan ilerleme) ya da Katman B'ye
(serbest kod uretimi, henuz yok) bagimli - hangisine oncelik verecegimizi
sor, otonom tahmin etme.

Her adimda: BUILD_OK + npm test gecti + canli tek cagriyla kanit
olmadan "bitti" sayma (K2). Sessiz catch birakma (bu oturumda 34 tane
bulunup duzeltildi, yeni kod da ayni standarda tabi). Checklist'i
guncel tut, commit mesajlarini detayli yaz, AETHER'a delta dus.
```
