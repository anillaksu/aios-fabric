# OTURUM KAYDI — 2026-08-18 (GÜNCEL — iki bölüm halinde)

**Bu dosya bu günün detaylı özetidir.** Güncel iş listesi her zaman
`docs/CHECKLIST.md`'dir — burası "ne olduğunu" anlatır, oradaki "şu an ne
durumda"yı gösterir. **§1-6 sabah/öğlen bölümü** (context %89'da kaydedilmişti,
commit `95292f2`'ye kadar); **§7'den itibaren devam bölümü** — aynı gün,
sonraki oturumda B-9 kontrolüyle başladı, `docs/KARARLAR_MIMARI_OZET_2026-08-18.md`
ile devam etti, W6.K/W6.L revize/M-9/B-9 kök neden/B-12'ye kadar gitti.

Önceki oturum kaydı: `docs/OTURUM_2026-08-17.md`.

---

## 1. Sabah/öğlen bölümünde ne oldu (özet — ayrıntı için önceki commit'lere bakın)

1. `docs/MIMARI_TEMEL.md` yazıldı — dört seviyeli kanıt skalası (FACT ›
   TEST-VERIFIED › REVIEW-VERIFIED › TARGET) kuruldu.
2. W6.I (Framework7 kaldırıldı, ~%90 shell küçülmesi), W6.B/C (WindowManager +
   gerçek artefakt lifecycle), M-5/M-7/M-8 (Artifact Contract kapısı), W6.F
   (IndexedDB göçü), B-10 (soğuk açılış tab bug'ı) kapandı.
3. `persist()` (Chrome IndexedDB'ye Shizuku üzerinden erişim) sonuna kadar
   kovalandı — SELinux/üretim-derlemesi sınırına çarpıldı, kanıtlandı, **TARGET
   kaldı, root gerekiyor** (owner kendi araştırıyor, fiziksel USB erişimi şart).
4. M-9 kararı: depolama yönü ters çevrildi (sunucu birincil, IndexedDB önbellek).
5. M-10: ham prompt/yanıt kaydı, sigortalı anahtar ("HAM KAYIT" Control Center'da).
6. B-5/7/8/10/11 kapandı; sessiz-catch denetimi (34 bulgu, 32 düzeltildi).
7. W6.L (prompt→artefakt önbelleği) ilk sürümüyle kuruldu — kanıt yolculuğunda
   B-9'un üçüncü tekrarı yanlış teşhise yol açtı (önbellek sanıldı, backend
   ölmüştü), servis yeniden başlatılıp FACT'e çıktı.

**Bu bölümün sonunda durum:** 80/121 madde tamam. Son commit `95292f2`.

---

## 2. Devam bölümü — bu günün ikinci yarısı

### 2.1 B-9 kontrolüyle başlandı (bu oturumun ilk işi)

SSH ile erişilip dört sürecin (fabric, llm_bridge, hermes gateway, watchdog)
ayakta olduğu doğrulandı — **bu kontrolde temiz çıktı**, yeniden başlatma
gerekmedi (ama gün ilerledikçe B-9 dört kez daha tetiklendi, aşağıya bkz.).

### 2.2 Owner'ın konsolide mimari karar özeti kaydedildi

`docs/KARARLAR_MIMARI_OZET_2026-08-18.md` yazıldı — KARAR-1/2/3, artifact
composability modeli (`CanCompose(A,B,Γ,Π)`), Ephemeral Execution Graph ≠
Persistent Artifact ayrımı, W6 A-K durumu, güvenlik/policy kararları, henüz
karar verilmemiş açık maddeler tek yerde toplandı. Yeni karar içermiyor,
çelişkide `CHECKLIST.md`/`PLAN_W6_app-shell.md`/`MIMARI_TEMEL.md` kazanır.

### 2.3 W6.K KAPANDI — Web Worker çekirdeği (commit `9dcff13`)

`extractArtifacts()`/`actionableCount()`/`admitArtifact` akışı ana thread'den
izole bir Worker'a (`parse-worker.js`) taşındı; saf compute
`artifact-parse.js`'e ayrıldı (DOM'suz, Node'da test edilebilir);
`parse-client.js` Worker'ı `WindowManager`'la aynı enjeksiyon desenini
kullanarak yönetiyor — kaçak/asılı worker `terminate()` ile öldürülüyor
(otomatik test edildi). 10 yeni test, owner iki yoldan (HERMES `ask()`,
ARTEFAKT `fillWindow()`) canlı doğruladı. **FACT.**

### 2.4 W6.L REVİZE — cache yazma uygunluğu `writeOrigin`'e bağlandı (commit `2ef0351`)

**Bulgu:** HERMES sekmesinde ikinci mesajdan sonra `history` bir daha hiç
boşalmıyor, eski kural (`cacheable = history.length===0`) cache'i sohbet
boyunca kalıcı kapatıyordu. **Bu bir DAG/Artifact Compiler eksikliği
DEĞİLDİ** — doğrulandı, `cacheKey()` hiçbir yapısal dedup kullanmıyor.
Owner'ın onayladığı model: READ her zaman denenir, WRITE yalnızca güvenilir/
standalone kaynaklardan (`writeEligible(trustedWrite)` — `fillWindow`/
`YENİLE` yazabilir, sohbet-içi genel `ask()` çağrıları yazamaz). 3 yeni test.
Owner iki senaryoyu canlı doğruladı: çapraz-yüzey HIT (ARTEFAKT→HERMES) ve
güvenli WRITE sınırı ("evet" cache'e hiç girmedi). **FACT.**

### 2.5 M-9 KAPANDI — sunucu birincil depolama, K5 kanıtı (owner onayıyla)

Telefona dokunmadan, sadece sunucuya `curl` ile işaretli bir test artefaktı
(`M-9 TEST MARKER`) eklendi (owner onayı alınarak — POST bir mutasyon,
otomatik mod sınıflandırıcısı haklı olarak durdurdu, izin istendi/verildi).
Owner uygulamayı yeniledi, marker'ı gördü — client'ın açılışta gerçekten
sunucudan okuduğu kanıtlandı (IndexedDB bunu asla bilemezdi). **FACT.**

### 2.6 10 test artefaktı eklendi + B-9 DÖRDÜNCÜ kez tetiklendi

Owner "10 tane daha artefakt yap, deneyeceğim" dedi. Gerçek capability'ler
kullanan 10 artefakt (fener, medya kontrolü, konum, bildirim, wakelock, tts,
3× risk:ask, app.list) sunucuya eklenirken **B-9 dördüncü kez vurdu** — SSH+
HTTP tamamen erişilemez oldu, dört sürecin dördü de ölmüştü.
`start_hermes_os.sh` ile yeniden başlatıldı, sonra 10 artefakt başarıyla
eklendi.

### 2.7 B-9 KÖK NEDENİ BULUNDU (kod/cihaza soruldu, tahmin edilmedi)

`~/.termux/boot/aios-boot.log`'da bugün **tek bir boot girişi yok** (son
giriş dün sabah 07:55), `uptime` bugün "1 gün+" gösteriyor. **Bugünkü dört
kesinti cihaz REBOOT'u değildi** — `Termux:Boot`/`10-aios.sh` bu kesintilerle
hiç ilgisizdi. Gerçek neden: **MIUI/HyperOS (Xiaomi, Android 15, build
V816)** cihaz açıkken Termux sürecinin tamamını arka planda öldürüyor — bu
bir kod sorunu değil, cihaz ayarı sorunu (persist()'in SELinux sınırıyla aynı
sınıf). Owner'a somut MIUI ayarları verildi (owner uygulayacak):
1. Güvenlik uygulaması → İzinler → Otomatik başlatma → Termux açık
2. Ayarlar → Uygulamalar → Termux → Pil → "Kısıtlama yok"
3. Son uygulamalar ekranında Termux kartını kilitle

Ayrıca `scripts/watchdog.sh` (repo) telefondaki canlı sürümden **geride**
olduğu bulundu (2026-08-16 ankraj düzeltmesi repoya hiç yansımamıştı) —
telefondan çekilip kanoniklendi. Üstüne gerçek bir eksiklik bulundu:
watchdog döngüsü yalnızca `fabric`+`llm_bridge`'i izliyordu, **`hermes
gateway` hiç izlenmiyordu** — üçüncü kontrol bloğu eklendi (commit `2ec5e41`).
**Canlı kanıt:** `hermes gateway` kasıtlı öldürüldü, 45sn içinde otomatik
geri geldiği hem `pgrep` hem `watchdog.log`'daki yeni satırla doğrulandı.
**B-9 durumu: kısmi `[~]`** — kod tarafı (hermes gateway izleme) FACT, MIUI
ayar değişikliği owner'ı bekliyor.

### 2.8 B-12 bulundu ve KAPANDI — veri döndüren capability sonuçları görünmüyordu

Owner 10 test artefaktını denedi: "Konum Bilgisi bir şey vermedi, Uygulama
Listesi de işe yaramadı." Journal'a sorulunca (tahmin değil): **her iki
capability de sunucu tarafında başarıyla tamamlandı** (gerçek GPS koordinatı,
gerçek uygulama listesi döndü) — sorun capability'de değil, `ctx.dispatch()`
`script.run` DIŞINDA hiçbir capability için sonuç gösterme koduna sahip
değildi. `public/js/dispatch-utils.js:hasMeaningfulData()` yazıldı (saf,
Node'da test edilebilir), `script.run`'ın kendi gösterim deseni (action-
receipt + mono metin + HERMES'e düşme) TÜM veri-döndüren capability'lere
genellendi — yeni bir desen icat edilmedi (K10). 5 yeni test. Owner butonlara
tekrar bastı, ikisi de HERMES'te gerçek sonuçla göründü. **FACT.**

### 2.9 B-13 bulundu — AÇIK, owner kararı gerektiriyor

Aynı 10-artefakt testinde journal'da görüldü: `risk:ask` capability'ler
(`activity.start`, `clipboard.set`) **kesin `task.failed` ile reddediliyor**:
`"onay gerektirir (risk: ask) - onay kuyrugu henuz baglanmadi, is
calistirilmadi"`. **KARAR-2'nin ("tek seferlik onay, kapsam değişince
yeniden sor") onay kuyruğu hiç kurulmamış** — bugünkü Katman A'da bile
`risk:ask` bildiren HİÇBİR capability çalışmıyor. Owner kararı gerektirir
(onay UI'sı nasıl görünecek, nerede tetiklenecek) — **kodlama başlatılmadı**.

---

## 3. Şu an durum (context sıkışmadan önce kaydedildi)

- **Depo:** `C:\Users\anil\Desktop\aios-fabric`, son commit `79a9c05`, `git status` temiz
- **Telefon:** `deploy-to-phone.sh --check` → depo ile birebir aynı (**52 dosya**)
- **Testler:** `fabric/test/*.test.ts` — **54/54** geçiyor (PC + telefon)
- **Checklist:** **82 tamamlanan / 33 kalan / 8 kısmi** (`docs/CHECKLIST.md`, 123 madde)
- **Bugün kapanan:** W6.K (FACT), W6.L revize (FACT), M-9 (FACT), B-12 (FACT),
  B-9 hermes-gateway-izleme dilimi (FACT, MIUI ayarı owner'ı bekliyor)
- **Bugün açılan/bulunan yeni borçlar:** B-12 (kapandı), B-13 (açık, owner kararı)

### ⚠️ En önemli açık risk: B-9 (bugün DÖRT kez daha tekrarladı, toplam altı)

Kök neden artık NET: **MIUI'nin süreç öldürmesi, reboot değil.** Owner'ın
telefonda 3 ayar değiştirmesi bekleniyor (§2.7). Sonraki oturuma başlarken
önce kontrol et:

```
KEY="$HOME/Desktop/Telefon_AI_Agent_Session_2026-08-16/keys/phone_termux_key"
ssh -p 8022 -i "$KEY" u0_a322@100.75.177.88 'echo CANLI'
ssh -p 8022 -i "$KEY" u0_a322@100.75.177.88 'ps -ef | grep -E "uvicorn|hermes|node.*server.ts|watchdog" | grep -v grep'
```

Erişilemezse ya da bir süreç eksikse: `nohup bash ~/start_hermes_os.sh > ~/start_hermes_os.out 2>&1 & disown`

MIUI ayarları hâlâ değiştirilmediyse, owner'a hatırlat (§2.7'deki 3 adım).

### Ortam bilgisi (değişmedi)

- Telefon Fabric: `http://100.75.177.88:9300` (Tailscale), SSH `u0_a322@100.75.177.88:8022`
- PC agent: `http://100.109.236.30:9310`
- SSH key: `$HOME/Desktop/Telefon_AI_Agent_Session_2026-08-16/keys/phone_termux_key`
- Cihaz: Xiaomi, MIUI/HyperOS, Android 15, build V816 (B-9 kök nedeniyle ilgili yeni bilgi)

## 4. Bu günün kalıcı kayıtları (dosya haritası, devam bölümü)

| Dosya | İçerik |
|---|---|
| `docs/CHECKLIST.md` | **Tek doğruluk kaynağı** — 82/123 madde |
| `docs/KARARLAR_MIMARI_OZET_2026-08-18.md` | Owner'ın konsolide mimari karar özeti (yeni karar yok, referans) |
| `fabric/public/js/artifact-parse.js`, `parse-worker.js`, `parse-client.js` | W6.K — izole Worker |
| `fabric/public/js/prompt-cache.js` (`writeEligible`) | W6.L revize |
| `fabric/public/js/dispatch-utils.js` (`hasMeaningfulData`) | B-12 |
| `scripts/watchdog.sh` | Kanonikleştirildi + hermes gateway izleme (B-9) |
| `fabric/test/parse-worker.test.ts`, `dispatch-utils.test.ts`, `prompt-cache.test.ts` (+3 test) | 54/54 |

## 5. Bu günün kalıcı dersleri

1. **"X çalışmıyor" semptomunun kök nedeni görünüşteki (DAG/compiler
   eksikliği) ile aynı olmayabilir** — W6.L'nin cache-miss'i DAG değil,
   `history`-tabanlı bir kapı sorunuydu; önce journal/koda soruldu.
2. **Canlı testten önce runtime bağımlılıklarını doğrula** — B-9 dört kez
   aynı günde vurdu, her seferinde "özellik mi bozuk yoksa backend mi ölü"
   sorusu önce sorulmalı.
3. **Kanıt için mutasyon gerekiyorsa önce izin iste** — sunucuya test verisi
   yazmak otomatik mod sınıflandırıcısı tarafından haklı olarak durduruldu,
   owner'dan açık onay alındı, sonra yapıldı.
4. **Bir cihaz/OS sınırı bulununca (SELinux, MIUI) kod tarafında daha fazla
   kovalama yapılmaz** — owner'a somut, uygulanabilir adımlar verilip
   devredilir.
5. **Var olan bir deseni genelleştirmek, yeni desen icat etmekten iyidir**
   (K10) — B-12'de `script.run`'ın gösterim deseni aynen genellendi.

---

## 6. GERİ DÖNÜŞTE KULLANILACAK PROMPT

Aşağıdaki metni bir sonraki oturumun başında olduğu gibi yapıştırın:

```
AI-OS Fabric'e devam ediyoruz. Önce mcp__aether__aether_handoff
(aether://project/aether-fabric) çağır, sonra docs/OTURUM_2026-08-18.md
ve docs/CHECKLIST.md'yi oku. Çalışma dizini C:\Users\anil\Desktop\aios-fabric.

ONCE B-9'U KONTROL ET (docs/OTURUM_2026-08-18.md §3'teki komutlarla). Kok
nedeni artik NET: MIUI'nin surec oldurmesi, reboot degil. Owner'in telefonda
3 ayar degistirmesi bekleniyor (Otomatik baslatma / Pil kisitlamasi yok /
Son uygulamalarda kilit) - hala yapilmadiysa hatirlat.

Durum: 82/123 checklist maddesi tamam. Bugun W6.K, W6.L revize, M-9, B-12
kapandi (hepsi FACT, canli kanitli). B-9'un hermes-gateway-izleme dilimi de
FACT ama MIUI ayari owner'i bekliyor.

Acik en onemli is: B-13 - risk:ask capability'lerin onay kuyrugu hic
kurulmamis (KARAR-2 tasarlandi ama kodlanmadi). Bugun activity.start/
clipboard.set gibi TUM risk:ask capability'ler "onay kuyrugu henuz
baglanmadi" hatasiyla reddediliyor. Owner karari gerektirir (onay UI'si
nasil gorunecek, nerede tetiklenecek) - otonom tahmin yurutme, once sor.

Kalan checklist maddelerinin cogu (W6.G/N/O/P/V/Z, S-4/5/7/8) ya urun
karari gerektiriyor ya Katman B'ye bagimli - hangisine oncelik verecegimizi
sor.

Her adimda: BUILD_OK + npm test gecti + canli tek cagriyla kanit olmadan
"bitti" sayma (K2). Sessiz catch birakma. Checklist'i guncel tut, commit
mesajlarini detayli yaz, AETHER'a delta dus.
```
