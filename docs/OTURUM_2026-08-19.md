# OTURUM KAYDI — 2026-08-19

## Agent-bağımsız devir katmanı (IN_PROGRESS, FACT değil)

`AIOS_DETERMINISTIC_HANDOVER.md` eklendi ve `AIOS_SESSION_START.md` ile
canonical handoff'a bağlandı. Amaç, chat/session belleği olmadan her agentin
HEAD → worktree → source → test → live evidence sırasıyla aynı bağlamı yeniden
kurmasıdır. `PRODUCT_GAP_REGISTER.md` açık ürün borcunu `gapId` ile taşır;
kayıt silinmez, status/kanıt ilerler.

Bu aynı turda Exact Formation Reuse Explorer için uncommitted UI düzeltmesi
vardır: artifact kartı ActionCard'ın gerçek `action` sözleşmesine bağlandı,
identity satırları ayrıldı, Canvas toolbar/zoom ve workspace route projection
iyileştirildi. Yerelde `npm test` **158/158** ve `BUILD_OK` geçti; telefon
kaynakları yeni sürümle eşleşti. Owner görsel kabulü ve canonical commit
olmadan bu UI düzeltmesi **TEST-VERIFIED**'dır, FACT değildir.

Telefon salt-okunur kontrolünde Fabric + watchdog online, `llm_bridge` ve
Hermes gateway down gözlemlendi. Bu anlık gözlem B-9'un tarihli önceki sağlık
kanıtını değiştirmez; bugünkü runtime sağlığı için yeni FACT de üretmez.

## Cross-platform fresh-install ve bağlantı denetimi (TEST-VERIFIED)

Repository taramasında platform kaçakları açıkça ayrıldı: Android device
runtime `capabilities.ts` ve başlatıcılarda Termux binary/pathlarına bağlı;
`deploy-to-phone.sh` owner telefon/key varsayılanı taşır; Fabric ve PC peer
self URL'leri environment ile override edilebilir; Hermes gateway ile
`llm_bridge` external Termux/proot çalışma alanıdır ve source checkout içinde
yoktur. Ayrıca testte `server.ts`nin `$HOME/fabric` deployment deposuna token
yazmayı denediği ve Windows source checkout'ı generic Fabric runtime kabul
etmediği gözlendi. Git tarafından izlenen tipik private-key/token imzaları tarandı;
değer ifşa edilmeden eşleşme bulunmadı.

`CROSS_PLATFORM_BOOTSTRAP.md` Windows observer, Windows PC peer,
Ubuntu/Linux observer ve Android/Termux device runtime rollerini birbirine
eşitlemeden belgeliyor. `fabric/bin/aios-setup-doctor.mjs` yalnız prerequisite
okur; servis/token/peer/capability üretmez. `setup-doctor.test.ts` ile
observer kabulü, desktop'ın Termux sayılmaması, optional Shizuku sınırı ve
fail-closed argüman sözleşmesi test edildi. Yerel Windows observer canlı
çıktısı kabul edildi; toplam `npm test` **162/162**, `BUILD_OK`.

Bu yalnız observer/setup doctor zinciridir. Ubuntu fresh device runtime,
Termux fresh runtime, Windows peer canlı round-trip ve public release
paketleme **FACT değildir**; `PG-021` altında açık kalır.

## Yeniden araştırma arşivi

Bu oturumda kod yazmadan önce repository yeniden temellendirildi:

- HEAD: `6e916c2212e621c784de9d952280314527163cf5`
  (`W6: add workspace data states and recovery`).
- Çalışma ağacı başlangıçta temizdi.
- `CHECKLIST.md`, `MIMARI_TEMEL.md`, `AIOS_HANDOFF_CURRENT.md`,
  `OTURUM_2026-08-18.md`, güncel commit zinciri, ScreenSpec referans
  artefact'ları, ApplicationEntry modeli ve capability registry çapraz
  okundu.

### Bulgu

Repository kararları, oturum kronolojisi ve FACT zincirlerini taşıyor; ancak
bir owner kararı + agent değişikliği + cihaz gözleminin hangi yeni ekran ya da
akışta yeniden kullanılacağını gösteren ortak indeks yoktu. Bu nedenle
`docs/ARASTIRMA_ARSIVI.md` oluşturuldu.

Arşiv yeni runtime protokolü, veri deposu, authority veya execution yolu
değildir. Aşağıdakileri değişmeden bağlar:

- `CHECKLIST.md` madde durumunun tek doğruluk kaynağı oluşunu,
- FACT = commit + test + canlı kanıt kuralını,
- dispatcher/policy ve dar `/read` sınırını,
- Artifact ≠ ApplicationEntry ≠ Android uygulaması ayrımını,
- B-9'un anlık sağlık ile MIUI survivability arasındaki ayrımını.

### `6e916c2` durumu

Bu commit için kod/test/dağıtım kanıtı vardır: `npm test` 89/89, telefon
BUILD_OK, Fabric 200 ve 68 dosya md5 eşitliği. Normal uygulama listesi ve
KEŞFET araması telefon üzerinde kabul edildiği bildirildi; fakat hata/"Tekrar
dene" yolu kontrollü gerçek servis kesintisiyle kanıtlanmadı. Bu yüzden
arşivde error/retry yalnız **TEST-VERIFIED** olarak kaldı; bu kayıt yeni bir
FACT iddiası değildir.

### CURRENT DECISION POINT

Phone Workspace için sonraki görünür ürün dilimi owner tarafından seçilir.
En yüksek mevcut yeniden kullanım potansiyeli: gerçek Android uygulama
başlatıcısı (mevcut `app.list` + `app.open` + recent) ve mevcut
volume/brightness/torch/Shizuku capability'lerini kullanan Hızlı Ayarlar
yüzeyidir. Bu öneri karar değildir; kodlamadan önce kartlardaki sınırlar ve
gerçek cihaz capability cevapları yeniden doğrulanacaktır.

## Portable CLI ve Bağlantı Köprüsü kabulü

`55c4d62` ile Node 22+ tabanlı, salt-okunur `aios` CLI eklendi; `status`,
`capabilities`, `artifacts`, `formations` dışında yüzey açmaz. Ortak olan
şey shell değil CLI sözleşmesidir: argv, stdout, stderr ve exit code.
Termux/Android başlatma ve cihaz komutları adapter olarak kaldı; CLI
dispatcher dışı execution oluşturmaz.

Windows ve gerçek Termux'ta `aios --json formations` canlı çalıştırıldı.
Çıktı SHA-256'ları aynıydı:
`7c711043a31ecbab7a0e9f91ed29125db23d8659f62d3f758d7adb6ad1e0adac`.
Yerel 151/151 + BUILD_OK; telefonda 151/151 + BUILD_OK geçti.

`0cb9db9` ve `3b6c579`, Bridge sözleşme testinin kaynak depo ve Termux Widget
konumlarını doğru çözmesini sağlar. Köprü `ss`/`netstat` yerine önce gerçek
TCP probe (`nc -z -w 1 127.0.0.1 8022`) yapar; canlı widget çağrısı
`bridge_exit=0`, `tcp_listener_exit=0` ve `sshd zaten dinliyor` sonucunu
verdi. Mevcut elle başlatılmış sshd portu tuttuğundan runit'in port kaybı
sonrası devralması kesinti oluşturmadan test edilmedi; B-9 açık risk kalır.

## Node admission / platform yayılım denetimi

`289dad3`, mevcut CLI'a `aios node doctor` ekledi. Bu komut yeni node/peer
protokolü veya execution yolu değildir: A2A Agent Card, runtime status ve
capability discovery'nin üç salt-okunur kontrolünü sabit sırada yapar;
eksik olan varsa exit `3` ile fail-closed döner.

Windows x64 üzerinden telefon endpointi ve telefonun Termux Android/arm64
runtime'ı ayrı ayrı canlı geçti. Her iki tarafta A2A `1.0`, 4 runtime servisi
ve 39 capability görüldü; yerel+telefon `npm test` 153/153 ve BUILD_OK geçti.
Mevcut registry Termux ikililerine bağlı olduğundan bunun Linux/macOS/Windows
native runtime FACT'i olmadığı özellikle kaydedildi. Dağıtım sınırı ve sonraki
owner seçimi `NODE_DISTRIBUTION_TARGET.md` içindedir.

## Cloudflare oluşum ağı ön denetimi

Owner, oluşumların insan → cihaz → capability → oluşum → doğrulanmış iz →
başka bağlamda kullanım döngüsünde bir ağ olarak büyümesini ve bunun
Cloudflare hesabında araştırılmasını istedi.

Salt-okunur denetim:

- `wrangler 4.108.0` kurulu; kullanıcı API token'ı ile `wrangler whoami`
  hesabı doğruladı.
- Bu depoda Cloudflare deploy/config yok.
- Hesapta iki mevcut KV namespace bulundu ama AIOS'a aitlik kanıtı yok;
  dokunulmadı.
- R2 etkin değil (`10042`); AIOS D1 veritabanı görülmedi.

Sonuç: Cloudflare'a veri, Worker, D1/R2 kaynağı ya da public endpoint
oluşturulmadı. Arşiv, döngüyü ve güvenli ilk pilot sınırını
`ARASTIRMA_ARSIVI.md §5-6` içinde TARGET olarak kaydetti. Bir sonraki owner
kararı yalnız erişim/veri sınırıdır: owner-only redakte Formation Index pilotu
mu, yoksa paylaşım/ekonomi tasarımı mı önce gelir? Para, token, marketplace
veya self-funding iddiası bu denetimde FACT yapılmadı.

## Kanonikleşen operasyon ve workspace kümeleri

- `8237ba9` — **TEST-VERIFIED:** kaynak varsayılan Gateway sırrı kaldırıldı,
  `/read` dar facade'sı tek fonksiyonda toplandı, LLM bağlamı yalnız
  dispatcher'ın trusted provider'ından eklenir, ham `/panel` ve uzaktan peer
  ekleme yüzeyi kaldırıldı. Bu commit yeni canlı FACT iddiası değildir.
- `a4b7b2d` — **FACT:** Termux:Widget provisioning, Hermes'in
  `gateway.platforms.api_server.extra.key` canonical store'u ile Termux
  `FABRIC_GATEWAY_KEY` ortamını sır göstermeden eşler. Owner Widget'ı canlı
  çalıştırdı; `fabric.env` mode `600` ve authenticated Gateway `/v1/models`
  yanıtı `200` ölçüldü. Bu, B-9'un MIUI/HyperOS survivability riskini kapatmaz.
- `385a7ed` — **TEST-VERIFIED:** persistent WindowManager dock, clipboard
  import sınırı, Night City atmosphere ve klavyesiz tam ekran Operator Deck.
  119/119 test, telefon BUILD_OK, HTTP 200 ve 85 dosya md5 eşitliği vardır.
  Operator Deck'in fiziksel telefon görünür kabulü beklenir; FACT değildir.
