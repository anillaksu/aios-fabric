# OTURUM KAYDI — 2026-08-20

Devir oturumu. Codex devam edemediği için repository üzerinden yeniden
bağlam kuruldu; sohbet özetine değil koda/teste/canlı çağrıya bakıldı.

## 0. Devralınan durum (kod yazmadan önce)

```text
HEAD      8fbbbf84221070e3fdc590b81c5200fa1361ccde  (master)
worktree  10 M + 2 ??  (Explorer/UI çalışması, uncommitted)
npm test  164/164 · npm run build BUILD_OK · git diff --check temiz
```

`AIOS_DETERMINISTIC_HANDOVER.md` algoritması uygulandı; `AIOS_HANDOFF_CURRENT.md`,
`PRODUCT_GAP_REGISTER.md`, `CHECKLIST.md`, `MIMARI_TEMEL.md`, `STANDARTLAR.md` ve
`OTURUM_2026-08-19.md` okundu. Devralınan uncommitted çalışma silinmedi, rebase
edilmedi, yeniden yazılmadı; karşılığı `PG-001`'dir.

AETHER oturum başında açıldı (`aether_info` → `aether_handoff`). **Bulgu:**
`aether://project/aether-fabric` düğümünün hedefi AETHER'ın kendi semantic fabric
implementasyonudur (SQLite graph, MCP adapter); AIOS ürün işi orada izlenmiyor ve
son aktivite `2026-08-11`. AETHER bu turda yardımcı bağlam olarak kullanıldı,
devir otoritesi olarak değil (`write_canonical=false`, `promote=false`,
`execute=false`).

## 1. Telefonun gerçek durumu

Tailscale `xiaomi-13-lite-1` aktif. İlk SSH denemesi `Connection refused` verdi;
sınır yazmadan tekrar denendi ve `SSH_OK` geldi — geçici bir durumdu.

`/runtime-status` bir ara `watchdog: down` derken `ps` süreci canlı gösterdi.
Kök neden bulundu ve `PG-023` olarak kaydedildi: çalışan kopya
`~/.shortcuts/watchdog.sh` (md5 `1130a903…`), kanonik `~/watchdog.sh`
(md5 `6e367f00…`, repo `scripts/watchdog.sh` ile birebir) değildi. Eski kopyanın
tek farkı Runtime Ledger `observe watchdog-cycle` bloğunun eksik olmasıdır.
`00:52:41Z`'de stack `start_hermes_os.sh` ile yeniden başlatılınca kanonik
watchdog (PID 19779) devraldı ve dört servis de `online` oldu. Uptime kesintisiz
`2 gün 20:08` — bu turda B-9 kaynaklı bir öldürme gözlenmedi.

Devralınan uncommitted UI'nin telefonda **zaten canlı** olduğu HTTP md5
karşılaştırmasıyla görüldü (37/37 public varlık birebir); `/read shizuku.status`
`200` döndüğü için sunucunun da yeni `capabilities.ts` ile çalıştığı doğrulandı.

## 2. Explorer'ın canlı veriyle doğrulanması

Telefonun `/formation-memory` ve `/artifacts` çıktıları çekilip mevcut
`projectFormationExplorer()` gerçek veriyle koşuldu:

- 22 formation, 15 provenance edge, **22/22** exact backing artifact.
- Doğrulanmış execution taşıyan 4 formation: El Feneri (5), Kaydırılabilir Ses
  Paneli (4), QR Tarayıcı (1), Telefon Uygulamaları (5).
- `cap:volume.set` filtresi 3 kayıt döndürdü; başlık/similarity filtresi
  fail-closed reddedildi.

## 3. Eksik kabul kriterlerinin kapatılması (`PG-001`)

Direktifin ürün yüzeyi maddelerine göre kodda gerçekten eksik olanlar kapatıldı:

- **Formation Detail → Canvas** bağı eklendi (Canvas → detay zaten vardı).
- **CONTEXT** bloğu ayrıldı: `provenanceKind`, `capabilitySetVersion`, `parents`
  yalnız kaynaktan taşınır. Canlı dağılım: `hermes:8, unknown:12, reference:2`.
- **Teknik ayrıntı drawer'a alındı:** ham `formationId`/`contentId`/`contextId`/
  `witnessId` ve per-execution `EDGE`/`WITNESS`/`TASK`/`DIGEST` kimlikleri artık
  `ui.formationIdentity` sheet'inde. Ana yüzey altı ayrı blok taşıyor.
- **Liste kimlik özeti:** canlı veride aynı başlıklı iki `El Feneri` var
  (`51fa3b54b43a` 5 kullanım, `cc19401b8c03` 0 kullanım) — ancak kısa exact
  kimlikle ayrışıyorlar.
- **Uydurulmayan alan:** "son doğrulanmış kullanım ANI". `createRuntimeWitness`
  gövdesi `schema/parent/task/correlation/capability/resultDigest/ledger`; zaman
  alanı yok ve determinizmi bozacağı için olamaz. `lastVerifiedUseAt` bilerek
  `null`, sheet'te "witness şemasında zaman alanı yok" yazıyor. Journal'dan zaman
  devşirilmedi (ikinci kaynak + kırpma penceresi = sahte değer).

Testler regex eşlemesinden yapısal doğrulamaya geçirildi: blok sırası, ham
hash'in ana yüzeyde bulunmaması, her eylemin `ui.*` olması, CONTEXT değerlerinin
kaynakla aynılığı. Ayrıca **`renderer.js:validateScreen` round-trip testi**
eklendi — izin listesinde olmayan bir eylem sessizce düşerdi ve bu ancak
telefonda görülürdü.

```text
yerel   npm test 168/168 · BUILD_OK · git diff --check temiz
telefon npm test 168/168 · BUILD_OK · sunucu 200
md5     depo == telefon (107 dosya)
```

## 4. Canlı reuse denemesi — BAŞARISIZ, kök neden bulundu

Owner'ın açık izniyle, Ses Paneli'nin **kendi contract'ıyla** ve exact
`formationId` ile gerçek `volume.set` gönderildi.

**Gerçekleşen:** cihaz gerçekten değişti — `music 106 → 60 → 106`, iki kez
bağımsız `volume.read` ile doğrulandı. Task'lar tamamlandı
(`cc41d5b5-110a-4ad2-91ff-8589c287fbdc`, `cec0d5b3-20b4-43f1-bce0-ac2d6790177f`).

**Gerçekleşmeyen:** yeni provenance edge yazılmadı (15 → 15). Yeni formation da
oluşmadı (22 → 22) — reuse semantiği doğru, sorun yazma yarısında.

Kök neden `fabric.log` üzerinden bulundu:
`[fabric:server:runtimeProvenance] runtime ledger gecersiz alan: 127` — bu bir
exit kodu değil, **ledger TSV'sinin 127. satırı**. Ayrıntı ve düzeltme planı
`PG-022`'dedir. Özet: yazıcı `status=missing` satırında `commandHash` ve
`processWitness` alanlarına `-` yazar; okuyucu bu iki alan için koşulsuz hash
ister. Telefondaki 145 satırlık ledger bağımsız tarandı: reddedilen satır sayısı
**2** (127, 128), ikisi de `missing`. Zincir bütünlüğü ve **tüm** event-hash'ler
bağımsız hesapla geçerli bulundu; `aios-runtime-ledger.sh verify` de `LEDGER_OK`
döndürüyor. Yani sorun zincir değil, okuyucunun alan regexidir.

`readLedger` dosyanın tamamını önce doğruladığı için `2026-08-20T00:42:42Z`'den
sonraki **her** provenance yazımı fail-closed. Satırlar append-only ve
hash-zincirli olduğundan durum kendiliğinden düzelmez.

**Doğru çalıştığı kanıtlanan:** `volume.read` için edge yazılmadı, çünkü o
capability Ses Paneli formation contract'ında yok
(`completed capability parent formation sozlesmesinde degil`) — parent seçimi
guard'ı beklendiği gibi davranıyor.

**Owner kararı (2026-08-20):** bu turda üretim provenance çekirdeğine
dokunulmayacak, ledger yazıcı sözleşmesi değiştirilmeyecek, geçmiş append-only
satırlar kesinlikle yeniden yazılmayacak. Bulgu `OPEN BUG/RISK — contract drift`
olarak kaydedildi; exact invariant farkı ve gelecekteki düzeltmenin kabul testi
`PG-022` içinde tanımlandı. Fail-closed davranış korunuyor ve FACT olarak
değiştirilmiyor.

## 5. Görsel kabulün neden alınamadığı

PWA'yı PC'den sürme denendi. Claude-in-Chrome uzantısı bağlanmadı (handoff'un
2026-08-18'de kaydettiği bilinen kararsızlık). Playwright telefonun Fabric'ine
Tailscale üzerinden ulaştı ve sayfa açıldı, fakat düz HTTP secure context
olmadığı için `crypto.subtle` yok:
`TypeError: Cannot read properties of undefined (reading 'digest')` —
`artifact-contract.js:58` → `app.js:2030`. SSH local-forward ile `localhost`
üzerinden denendi; Playwright ayrı ağ ad alanında olduğu için host'un
`localhost`'unu görmedi. Tünel kapatıldı.

Bu bir ürün hatası değil, PC'den erişim yolunun sınırıdır: telefonda PWA
`localhost` üzerinden açıldığı için secure context sağlanır. **Explorer'ın görsel
kabulü telefonda yapılmalıdır.**

## 6. Bu oturumun kanıt sınıflandırması

```text
Explorer/detail/reuse ürün yüzeyi      TEST-VERIFIED (+ telefon dağıtım paritesi)
Explorer canlı veri projeksiyonu       TEST-VERIFIED (gerçek 22 formation/15 edge)
volume.set gerçek cihaz etkisi         LIVE-VERIFIED (106→60→106, iki bağımsız read)
reuse → witness → edge zinciri         AÇIK BUG (PG-022 nedeniyle fail-closed)
watchdog launcher drift'i              AÇIK BUG/RISK (PG-023)
PG-001                                 IN_PROGRESS (yükseltilmedi)
```

Owner'ın telefon görsel kabulü **alınmadı**; hiçbir UI iddiası FACT sayılmadı.
