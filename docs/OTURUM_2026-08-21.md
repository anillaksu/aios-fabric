# OTURUM KAYDI — 2026-08-21

AIOS Universal Runtime / Reference Node oturumu. Owner direktifi: telefonu
"AIOS'un kendisi" değil **AIOS Android Reference Node** olarak ele almak,
çekirdeğin platformdan bağımsızlığını **koddan** kanıtlamak ve ilk Windows
portunu çıkarmak.

Bu oturum direktifin **tamamını bitirmedi**. Aşağıda ne kanıtlandığı ve
neyin TARGET kaldığı ayrı ayrı, dürüstçe yazılıdır.

## 1. Android Reference Node snapshot (2026-08-20T21:23Z) — FACT

```text
PC repo HEAD      b974ee3 (worktree temiz)
telefon           git deposu DEĞİL (deployment hedefi — README kuralı)
uptime            3 gün 16:39, kesintisiz
runtime servisleri fabric / llm_bridge / hermes_gateway / watchdog → dördü de online
runtime ledger    LEDGER_OK, 169 olay
formation         22
provenance edge   18
artifact          37 (22'si formation taşıyor)
capability        39
```

Süreçler: `node --experimental-strip-types src/server.ts` (Fabric),
proot içinde `hermes gateway run` + `uvicorn llm_bridge` (9201),
`bash ~/watchdog.sh`, runit `sshd`.

## 2. Termux continuity / wake-lock — REVIEW-VERIFIED

`termux-wake-lock` **üç** noktada alınıyor, koddan doğrulandı:

- `~/.termux/boot/10-aios.sh:28` — boot anında
- `~/start_hermes_os.sh:77` — stack başlatılırken
- `~/watchdog.sh:67` — watchdog döngüsünde (45sn periyot) sürekli tazeleniyor

Owner'ın kuralı korunuyor: wake-lock **CPU sleep engelleme** kanıtıdır,
"Android süreci sonsuza kadar yaşar" demek değildir. B-9 (MIUI'nin Termux'u
öldürmesi) ayrı ve açık risk olarak duruyor. Mevcut mekanizma yeniden icat
edilmedi, yalnız kanonikleştirildi.

## 3. Platform-nötr çekirdek ↔ Android adapter ayrımı — FACT

Sızıntı taraması (`termux|android|shizuku|rish|/data/data|am start|pm list|proot`):

| Katman | Dosya | Eşleşme |
|---|---|---|
| **Çekirdek** | `formation-memory.js`, `formation-canvas.js`, `formation-explorer.js`, `artifact-contract.js`, `navigation-state.js`, `application-model.js`, `screenspec.ts`, `runtime-provenance.ts`, `approval.ts`, `read-policy.ts` | **hepsi 0** |
| **Adapter** | `capabilities.ts` | **92** |
| Yüzey | `screens.js` / `app.js` / `server.ts` | 19 / 11 / 1 |

Android'e özgü kod gerçekten capability/adapter sınırında duruyor.

`formation-memory.js` ayrıca **hiçbir modül import etmiyor** ve yalnız
`Array.isArray`, `JSON.stringify`, `Object.hasOwn`, `Object.keys`,
`TextEncoder`, `crypto.subtle.digest` kullanıyor — hepsi standart.

## 4. Taşınabilirlik kanıtı — FACT (çift yönlü)

**A) Android → Windows** (gerçek üretim verisi): 22/22 formation ve 18/18 edge
Windows'ta doğrulandı; `import → re-export` hash'i byte-özdeş
(`7fbae450…a469d`); JOIN determinizmi (A,B / B,A / A,A,B) korundu.

**B) Windows → Android**: Windows'ta üretilen formation + witness + edge
telefonun **kendi çekirdek kopyasıyla** doğrulandı; hash byte-özdeş
(`2d74ff1b…ad2e0`); kimlik değişmedi.

**C) Bağımsız kimlik yeniden hesabı** — en güçlü kanıt: birebir aynı girdi
`win32/x64` ve `android/arm64` üzerinde **bağımsız** hesaplandı, altı kimlik
alanının **hepsi byte-özdeş** çıktı (değerler `PG-027`'de). Farklı OS, farklı
CPU mimarisi, aynı kimlik.

**Regresyon kapısı:** `test/platform-neutral-core.test.ts` bu altı değeri
sabitliyor, çekirdeğe `import`/`require`/`node:fs`/`fetch`/`document`/
`window`/`localStorage`/`process.env|platform|arch` sızmasını reddediyor.
**Aynı dosya iki platformda da geçiyor:** Windows 4/4, telefon 4/4.

Kapı: yerel ve telefon `npm test` **190/190**, `BUILD_OK`, md5 depo==telefon
(111 dosya).

## 5. Bu oturumda YAPILMAYANLAR (TARGET — iddia edilmiyor)

Direktifin şu bölümleri **başlamadı**; hiçbiri FACT değildir:

- **§4 İlk sistem artifact ailesi** (Home, App Market, Device Center,
  Capabilities, Formation Canvas, Settings, Runtime Center, Agent Center,
  Terminal) — üretilmedi.
- **§5 App Market** — yok.
- **§7/§8 Windows Electron node + Runtime Center** — yok. `PG-028` olarak
  TARGET kaydedildi.
- **§6 npm dağıtım modeli** — araştırılmadı.
- **§9 Machine snapshot ortak modeli** — EXISTS/PARTIAL/MISSING çıkarılmadı.
- **§14 iOS boundary audit** — yapılmadı.
- **§16 Session checkpoint productization** — yapılmadı (zaten bu tur için
  zorunlu değildi).

Gerekçe: direktif tek oturumda bitirilemeyecek genişlikte. En yüksek kaldıraçlı
ve §20'nin başarı kriterinin **temeli** olan parça (platform-nötrlük kanıtı)
uçtan uca kanıtlandı; kalanı kanıtsız üretmek yerine açıkça TARGET bırakıldı.

## 6. Açık risk (bu oturumda oluştu)

- **Telefon `sshd` (8022) düştü.** Fabric ve dört servis **sağlam** (HTTP 200,
  hepsi online) — yalnız SSH kanalı kapandı. Üç deneme + backoff başarısız.
- Sonuç olarak telefonda oluşturduğum geçici sonda dizini
  **`~/aios-portability-probe/`** (3 küçük dosya: `windows-export.json`,
  `verify-on-android.mjs`, `recompute-identity.mjs`) **silinemedi**. Üretim
  verisine dokunmuyor, ayrı dizinde duruyor; SSH döndüğünde
  `rm -rf ~/aios-portability-probe` ile temizlenebilir.
- Üretim Formation Memory / provenance deposuna **yazılmadı** — tüm doğrulama
  geçici dizinde ve salt-okunur yapıldı.
- Bu, PG-020 (B-9) ve PG-023 ile aynı sınıf operasyonel risk.

## 7. Kanıt sınıflandırması

```text
Android reference node snapshot        FACT
Termux wake-lock zinciri               REVIEW-VERIFIED (koddan; survivability FACT değil)
Platform-nötr çekirdek envanteri       FACT
Android adapter envanteri              FACT
Android → Windows taşınabilirlik       FACT (gerçek üretim verisi)
Windows → Android taşınabilirlik       FACT
Cross-platform kimlik özdeşliği        FACT (win32/x64 ↔ android/arm64)
İlk sistem artifact ailesi             TARGET (başlamadı)
App Market                             TARGET (başlamadı)
Windows/Electron node                  TARGET (PG-028)
Machine snapshot ortak modeli          TARGET (audit yapılmadı)
iOS boundary                           TARGET (audit yapılmadı)
telefon sshd düşmesi                   OPEN RISK
telefonda kalan sonda dizini           OPEN (temizlik bekliyor)
```
