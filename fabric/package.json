{
  "name": "phone-ai-os-fabric",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript orchestration/state spine - REFLEX/THOUGHT/AGENT dispatch, SQLite WAL journal, optimistic UI, A2A delegation.",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "start": "node --experimental-strip-types src/server.ts",
    "build": "for f in src/*.ts; do node --experimental-strip-types --check \"$f\" || exit 1; done; node --check public/js/app.js && node --check public/js/api.js && echo BUILD_OK"
  }
}
